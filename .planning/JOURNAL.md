# Journal
- 2026-09-11 | /flow-plan 4 --gaps + execute | 04-15 closed the REQ-08 arming gap; gaps: []. Smoke 267 tests/29 suites. The gap: on a cold relaunch with ONLY the geofence enabled the region could never arm — reference nil in a fresh process, registration needs one, and the sole recovery sat inside the disabled significant-change handler. Human widened D-12 (D-16) for a single last-ping coordinate, overwritten never appended, under 4 tested conditions. A throwaway probe SETTLED two RESEARCH unknowns by measurement: CLMonitor persists across a real process kill, and its events ARE delivered on the simulator — so the earlier non-reproduction was the bug, not the platform. Plan took 3 revision rounds: a protection constant that does not exist in the SDK, a double-optional `??` that would have failed the plan's own decisive test, a fallback asserting a symbol that was never declared, an unprobed guard, and a comment still asserting the pre-D-16 law | GATE
- 2026-09-11 | /flow-execute 4 | 14 plans, 63 commits, smoke 259 tests/28 suites, gaps: []. VERIFICATION found a defect 257 green tests missed: REQ-09's minimum interval was clamped, persisted and DISPLAYED but never reached the gate — `PingRateLimiter.setMinimumInterval` had zero callers, so the limiter kept 60s while the screen promised otherwise. Closed by 04-14. Execution also found a real 18-minute HANG: MapKit's `mapItems` does not resume on `cancel()`, so a `withTaskGroup` race never returned — a plan-checker had passed that construction on review. Ran SERIALIZED, not parallel: zero crossed commit attribution vs phase 03's four. Executing the plans exposed 4 plan-level defects review had not (guard comment-strip hole, a 1.2m arithmetic error that would have inverted REQ-06's boundary test, a case-sensitivity mismatch, a non-compiling RESEARCH sample) | GATE
- 2026-09-11 | D-15 | MapKit AUTHORIZED — human ruled the geocoder gate: "architecture should not be restricting you to deprecated tools". ARCHITECTURE gains MapKit `MKReverseGeocodingRequest` and a new Forbidden entry confining it to one file (reverse geocoding only — no map views, no MapKit types in the ping path); REQUIREMENTS:34's `[NEEDS CLARIFICATION]` marker is resolved and now also records that geocoding is best-effort and that it EGRESSES a coordinate to Apple. 3 plans revised (04-01 ban->confinement with 13 escape probes, 04-04 builds the provider on a 3s raced budget with every failure path landing on an empty label, 04-13 wires it); the other 10 byte-identical, verified by git. Re-check PASS on all 5 axes, 0 new issues. Deletion diff caught 3 unnamed truth removals in 04-04 — all superseded by 7 stronger ones, no safety property lost | CONTINUE
- 2026-09-11 | /flow-plan 4 --auto | PASS — 13 plans, 6 waves. Research first settled REQUIREMENTS' open geocoder marker ("confirm before phase 4 plans"): `CLGeocoder` is soft-deprecated at iOS 26.0, replacement is in MapKit, which is NOT on ARCHITECTURE's closed Frameworks list — so the FACT is settled and the AUTHORIZATION is not; phase ships empty labels until ruled. Checker returned 14 blocking issues, all closed in one round: 13 of the phase's verify steps passed at HEAD before any code was written (phase 02's exact failure, per LEARNINGS), the amended smoke guards would have failed the composition root two ways (`CLMonitorGeofence`, `UserDefaultsTriggerSettingsStore`), 04-07 was ordered to write a comment the un-comment-stripped ban would reject, SC-03/SC-04 were traced by nothing, and the two decisions PR #4 merged unresolved had no home. I re-ran the deterministic claims myself — wave-disjoint writes, requirement coverage, the 04-09 grep pipeline, MapKit absence — rather than trust the reports | GATE
- 2026-09-11 | /flow-next | PR #4 MERGED (c83a2c8) — phase 03 durable delivery is on main: queue store, backoff retry, connectivity observation, drain actor, and D-12's fifth wire key `at`. Smoke 177 tests/19 suites. Merged with two items the PR body itself marked "needs a decision" still unresolved — `QueueDrainCoordinator.hydrate()` copies queue entries into `PingHistoryLog` against ARCHITECTURE.md:72 ("never copied anywhere else"), and `UnqueuedPingSink` relabels a retryable 429 as permanent ("Failed: it is waiting"). Both carried into ROADMAP under phase 04 so the merge did not silently absorb them | CONTINUE
- 2026-09-11 | /flow-pr | PR #4 opened (https://github.com/jbrianfrancis-ir/GrokBotLocator/pull/4) — 7-lens review found 6 blocking defects after VERIFICATION said pass: 3 actor-reentrancy holes in one drain (delivered twice / erased a mid-drain ping / never removed what it delivered), a locked device destroying the whole queue via D-12's own protection class, an AX5 bar overflow, and 2 doc contradictions from D-13. All fixed with falsified tests, 169->177. Round 3 clean | GATE
- 2026-09-11 | acceptance 3 | phase 03 VERIFIED — 4 on-simulator checks PASS (queue on disk before the promise, `at` = fix time across a 56s process death, 401 never retried), 4 settled by D-14. Acceptance found a double-delivery bug 169 tests missed: actor reentrancy across `await`. D-13 shrank the type scale. Untested: connectivity-edge + background drain | CONTINUE
- 2026-09-10 | /flow-execute 3 | 12/12 plans, smoke 164 tests/19 suites, 45 truths VERIFIED, 0 gaps. Waves 3-6 serialized after wave 2's shared-index collisions — zero recurrences. Verifier re-probed all 5 smoke guards live. 3 backstops abstained, 8 human checks open | GATE
- 2026-09-10 | /flow-execute 3 | waves 1-2 of 6 done (5/12 plans), orchestrator gate green 123 tests/14 suites at 9fec0d7. Wave-2 finding: per-plan derived data isolates the log not the sources, so a plan cannot observe its own gate in a shared wave; and `git add -A` swept siblings' staged work 3x. Stopped at session turn cap, tree clean | CONTINUE
- 2026-09-10 | /flow-plan 3 | PASS — 12 plans, 6 waves, 2 revision rounds; 11 checker issues closed incl. a backoff table contradicting its own formula, one build log shared by every parallel plan in a wave, and 4 tasks gating on a tree that cannot compile. 3 backstop truths open; REQ-05's location wake carried to phase 04 | CONTINUE
