---
phase: 04-automatic-triggers
plan: 13
wave: 6
depends_on: [04-05, 04-09, 04-11, 04-12]
files_modified: [src/App/GrokBotLocatorApp.swift]
autonomous: true
requirements: [REQ-05, REQ-06, REQ-07, REQ-08, REQ-09, REQ-10, SC-04]
must_haves:
  truths:
    - "ONE PingRateLimiter instance reaches both PingModel and AutomaticPinger, so SC-04's ceiling is shared rather than doubled"
    - "TriggerCoordinator's drain closure calls QueueDrainCoordinator.drainForeground() — REQ-05's fourth drain opportunity is wired, not merely available"
    - "TriggerCoordinator.start() runs from the root view's task, a foreground touchpoint, so Always can be armed where the OS allows it to be armed"
    - "Scene phase drives presence: active marks the user present, background and inactive mark them absent, so a location wake drains silently and a foreground return still announces"
    - "With Application Support unavailable (no queue, no QueueDrainCoordinator) the triggers still arm and still ping, and the drain closure is a harmless no-op — phase 03's fallback is not regressed"
    - "Every concrete CoreLocation, Keychain, URLSession and UserDefaults type is still named only here"
  artifacts: [src/App/GrokBotLocatorApp.swift]
  key_links:
    - "GrokBotLocatorApp builds LocationDelegateProxy, CLMonitorGeofence, UserDefaultsTriggerSettingsStore, AutomaticPinger and TriggerCoordinator, and hands TriggerCoordinator to SettingsModel as its TriggerControlling"
user_setup:
  - "Grant Always when prompted on the device, to exercise the background-wake paths; a device is also the only place REQ-06/07/08's acceptance clauses and SC-03's battery figure can be observed."
---

<objective>
Wire phase 04 into the one composition root: one shared rate limiter, one trigger coordinator, the
location wake reaching the existing drain, and scene phase telling the drain whether anyone is there.
</objective>

<context>
src/App/GrokBotLocatorApp.swift — read its header first. It explains why the graph is built as
locals in an explicit `init()` and assigned through the underscored `@State` projections (a
`@State` default expression cannot reference another property of the same struct), and why the
SAME transport instance reaches both the sender and the drain. The same reasoning governs the
single `PingRateLimiter` this plan shares.
src/App/QueueDrainCoordinator.swift (04-05) — `setUserPresent(_:)`, `drainForeground()`.
src/Triggers/: LocationDelegateProxy (04-07), CLMonitorGeofence (04-08), AutomaticPinger (04-10),
TriggerCoordinator (04-11). src/Settings/: UserDefaultsTriggerSettingsStore (04-06), SettingsModel
with its `triggers:` parameter (04-12). src/Triggers/TriggerLabel.swift (04-04) —
`EmptyTriggerLabelProvider` is what ships; do NOT wire a geocoder, and do not import MapKit.
.planning/phases/04-automatic-triggers/RESEARCH.md consequence 4 — Always is only effective while
a session is held and one can only be started in the foreground, which is why `start()` is called
from the root view's `.task` and not from `init()`.
Gate: SMOKE_DERIVED_DATA=build/dd-04-13 ./scripts/smoke.sh.
In-task vs fan-in (.planning/LEARNINGS.md): the grep and presence assertions in every `<verify>`
below are checkable in-task and are what this plan is accountable for. `smoke exits 0` — and any
`Suite … passed` log assertion, which needs the build to complete — is NOT reliably observable
mid-wave: per-plan `SMOKE_DERIVED_DATA` isolates the build LOG, not the SOURCES, so a whole-project
build compiles every sibling's in-flight edits. Run it and report verbatim what it said; a failure
traceable to a sibling's half-landed edit is deferred to the orchestrator's fan-in gate, and is
never licence to change this plan's own files to go green.
</context>

<tasks>
<task type="auto">
  <name>Task 1: build the trigger graph in init()</name>
  <files>src/App/GrokBotLocatorApp.swift</files>
  <action>In `init()`, after the existing queue/coordinator construction:
- Keep the single `let rateLimiter = PingRateLimiter()` 04-09 introduced as a local and reuse it below — construct it exactly once in the whole file.
- `let proxy = LocationDelegateProxy()`, `let geofence = CLMonitorGeofence()`, `let triggerStore = UserDefaultsTriggerSettingsStore()`, `let labels = EmptyTriggerLabelProvider()`.
- `let pinger = AutomaticPinger(sender: sender, labels: labels, rateLimiter: rateLimiter, model: _pingModel.wrappedValue, now: { Date() })` — the same `sender` the button uses, so an automatic ping reaches the same credentials, transport, classifier and durable sink.
- Capture the queue drain coordinator that was just built into a local (`let queueCoordinator = _coordinator.wrappedValue`) and build `let drain: @Sendable () async -> Void = { await queueCoordinator?.drainForeground() }`. When Application Support was unavailable, `queueCoordinator` is nil and the closure does nothing — the triggers still arm and still ping, which is phase 03's documented fallback shape and must not regress.
- `_triggerCoordinator = State(wrappedValue: TriggerCoordinator(source: proxy, geofence: geofence, pinger: pinger, fixes: fixes, settingsStore: triggerStore, drain: drain))` against a new `@State private var triggerCoordinator: TriggerCoordinator?`. `fixes` is the SAME `CoreLocationFixProvider` the manual path already uses — the geofence-exit path needs a one-shot fix and there is no reason for a second provider.
- Pass `triggers: _triggerCoordinator.wrappedValue` into the `SettingsModel(...)` construction.
Update the file header: name the new dependencies, say that exactly one `PingRateLimiter` is shared by the manual and automatic paths and why (SC-04 counts them together), and that `EmptyTriggerLabelProvider` is the deliberate shipping choice while REQUIREMENTS.md:34's geocoder clarification is open.</action>
  <verify>Presence first — `grep -c 'PingRateLimiter('` already prints 1 after 04-09, so alone it proves nothing this plan did (plan-format): `grep -c 'TriggerCoordinator(' src/App/GrokBotLocatorApp.swift` prints 1, `grep -c 'AutomaticPinger(' src/App/GrokBotLocatorApp.swift` prints 1, `grep -c 'CLMonitorGeofence()' src/App/GrokBotLocatorApp.swift` prints 1, and `grep -c 'EmptyTriggerLabelProvider()' src/App/GrokBotLocatorApp.swift` prints 1. THEN the single-instance and absence checks: `grep -c 'PingRateLimiter(' src/App/GrokBotLocatorApp.swift` prints 1, and `grep -rn 'import MapKit' src --include='*.swift'` finds nothing (exit 1). Then `SMOKE_DERIVED_DATA=build/dd-04-13 ./scripts/smoke.sh` exits 0 — note this file now names `CLMonitorGeofence` and `UserDefaultsTriggerSettingsStore`, which pass only because 04-01 strips both type-name tokens before its CLMonitor and UserDefaults guards match.</verify>
  <falsify>Construct a second `PingRateLimiter()` inline in the `AutomaticPinger(...)` call instead of passing the local: the count becomes 2 and the check fails (and the shared window SC-04 depends on is gone). Revert.</falsify>
  <done>One limiter, one sender, one fix provider, one trigger coordinator — all named only here.</done>
</task>

<task type="auto">
  <name>Task 2: the foreground touchpoint and scene phase</name>
  <files>src/App/GrokBotLocatorApp.swift</files>
  <action>In `body`:
- Extend the root view's `.task` to `await coordinator?.start()` then `await triggerCoordinator?.start()`. Order matters and should be commented: hydrating and draining the queue first means a trigger that fires immediately is not racing the launch drain, and `start()` on the trigger coordinator is the FOREGROUND touchpoint where Always may be armed (RESEARCH consequence 4). Never call `triggerCoordinator.start()` from `init()`.
- In the `onChange(of: scenePhase)` handler: on `.active`, call `coordinator?.setUserPresent(true)` BEFORE the existing `Task { await coordinator?.drainForeground() }` so that drain announces; on `.background`, call `coordinator?.setUserPresent(false)` before the existing `scheduleRefresh()`; add an `.inactive` arm that also calls `setUserPresent(false)`. Comment that false is the value a location wake must see — a process that came up for a location event never passes through `.active`, and 04-05's default is false for the same reason.
Leave `scheduleRefresh()` and the `.backgroundTask(.appRefresh(...))` scene modifier exactly as they are.</action>
  <verify>`grep -c 'setUserPresent' src/App/GrokBotLocatorApp.swift` prints 3 (one true, two false). Then this, which pins the CALL SITE rather than the mere presence of the call — a plain grep matches wherever the line sits, including `init()`:
```
python3 - <<'PY'
import re, sys
s = open('src/App/GrokBotLocatorApp.swift').read()
head, _, tail = s.partition('var body')
assert 'triggerCoordinator?.start()' not in head, "start() is called before `var body` (init) - it must run from the root view's .task"
m = re.search(r'\.task \{(.*?)\n\s*\}', tail, re.S)
assert m and 'await triggerCoordinator?.start()' in m.group(1), "start() is not inside the root view's .task"
print('trigger start wired in .task only')
PY
```
exits 0 and prints that line. Then `SMOKE_DERIVED_DATA=build/dd-04-13 ./scripts/smoke.sh` exits 0.</verify>
  <falsify>Move the `await triggerCoordinator?.start()` line from the `.task` into `init()`: the script's first assertion fires (`start() is called before \`var body\``) and it exits non-zero. Revert.</falsify>
  <done>Triggers arm from the foreground; presence tracks the scene.</done>
</task>

<task type="auto">
  <name>Task 3: end-to-end check on a device</name>
  <files>src/App/GrokBotLocatorApp.swift</files>
  <action>No behaviour change. Add a short doc comment listing the end-to-end runs this phase is finished by, cross-referencing where each is already written up: REQ-06/07 in TriggerCoordinator.swift, REQ-08 in GeofenceMonitor.swift, REQ-09 and REQ-10 in SettingsView.swift, and — the one this file owns — REQ-05's fourth drain opportunity end to end: airplane mode on, tap I'm here so a ping queues, background the app, turn airplane mode off, drive a location wake, and confirm the queued ping goes out with no announcement and the history row reads Sent when the app is next opened.</action>
  <verify><human-check>Run the REQ-05 fourth-drain sequence above on a device and report what the history row read when the app was reopened, and whether anything was announced while the app was in the background.</human-check></verify>
  <done>The end-to-end run is written down. No `<falsify>`: the verify is a human-check, which plan-format exempts.</done>
</task>
</tasks>
