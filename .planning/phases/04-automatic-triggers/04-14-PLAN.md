---
phase: 04-automatic-triggers
plan: 14
wave: 7
depends_on: [04-09, 04-11, 04-12, 04-13]
files_modified: [src/Triggers/TriggerCoordinator.swift, src/App/GrokBotLocatorApp.swift, tests/TriggerCoordinatorTests.swift, tests/SettingsModelTests.swift]
autonomous: true
requirements: [REQ-09, SC-04]
must_haves:
  truths:
    - "A minimum interval chosen in Settings is the interval the GATE enforces: after setting twice the default through SettingsModel, a claim 1.5 default-intervals after an allowed one is REFUSED — at the old wiring that claim was allowed"
    - "An interval already on disk is applied to the gate at startup, with no Settings visit: a coordinator whose store holds twice the default enforces it after start() — a relaunch does not silently revert to the default"
    - "Exactly one PingRateLimiter is constructed in the composition root and that same instance reaches PingModel, AutomaticPinger and TriggerCoordinator — SC-04 still counts manual and automatic in one window"
    - "The 15 s floor is still unreachable and still defined once: PingRateLimiter.hardFloor is the only definition, and neither changed file restates 15 or 60 as a literal"
  backstop_truths: []
  artifacts: [src/Triggers/TriggerCoordinator.swift, src/App/GrokBotLocatorApp.swift, tests/TriggerCoordinatorTests.swift, tests/SettingsModelTests.swift]
  key_links:
    - "TriggerCoordinator.applySettings calls PingRateLimiting.setMinimumInterval(s.minimumIntervalSeconds) — THE call that carries the stored value to the enforcer, on both the start() path and the update() path"
    - "GrokBotLocatorApp builds the one PingRateLimiter from triggerStore.load().minimumIntervalSeconds and passes that instance to PingModel, AutomaticPinger and TriggerCoordinator"
---

<objective>
Close VERIFICATION's one gap: the minimum interval is persisted (04-06) and displayed (04-12) but
never reaches `PingRateLimiter`, so the screen shows a value the gate does not honour.
</objective>

<context>
.planning/phases/04-automatic-triggers/VERIFICATION.md — the gap, and the learning that names this
bug's shape: storing, displaying and enforcing are three wirings; a plan must name the call that
carries a value from the type that stores it to the type that enforces it.
src/Core/Ping/PingRateLimiter.swift — `PingRateLimiting` (`claim`, `setMinimumInterval`),
`hardFloor`, `defaultInterval`. Its setter already clamps: never restate 15 or 60.
src/Triggers/TriggerCoordinator.swift:89 `applySettings` — reached from `start()` (disk) and
`update(_:)` (Settings). src/App/GrokBotLocatorApp.swift:87 — the one `PingRateLimiter()`.
src/Settings/SettingsModel.swift:246 `setMinimumInterval` — the path SettingsView's Stepper uses.
tests/TriggerCoordinatorTests.swift `makeCoordinator`; tests/GeofenceMonitorTests.swift
`InMemoryGeofence`. Fakes there are internal and reusable across files.
Hazards (.planning/LEARNINGS.md): a fake that returns without suspending cannot test an actor —
so both new tests drive the REAL `PingRateLimiter`, never a fake limiter. Nothing sleeps: time
enters as a `Date` parameter to `claim(at:)`.
Do not touch the three open backstops (04-05 hydrate/D-12, 04-10 429/D-14, 04-04 MapKit).
Gate: SMOKE_DERIVED_DATA=build/dd-04-14 ./scripts/smoke.sh; log at build/dd-04-14.log.
</context>

<tasks>
<task type="auto">
  <name>Task 1: TriggerCoordinator carries the stored interval to the gate</name>
  <files>src/Triggers/TriggerCoordinator.swift, src/App/GrokBotLocatorApp.swift, tests/TriggerCoordinatorTests.swift</files>
  <action>Give `TriggerCoordinator` a `private let rateLimiter: any PingRateLimiting`, as an init parameter WITHOUT a default (a default is how a call site silently reacquires the gap), placed after `settingsStore`. Make the FIRST statement of `applySettings(_ s:)` — above the authorization request, so a refused Always still applies the interval — `await rateLimiter.setMinimumInterval(s.minimumIntervalSeconds)`. Both entries into `applySettings` then carry it: `start()` (value loaded from disk) and `update(_:)` (value just changed in Settings). Add a doc line saying this is the seam between the stored value and the enforcer, and that the limiter's own setter owns the clamp — this file states no interval literal.
Update the app's construction at GrokBotLocatorApp.swift:125 to pass `rateLimiter: rateLimiter` — the SAME local already given to `PingModel` and `AutomaticPinger`, never a new one.
In tests, extend `makeCoordinator` with two defaulted parameters — `minimumIntervalSeconds: TimeInterval = PingRateLimiter.defaultInterval` and `rateLimiter: any PingRateLimiting = PingRateLimiter()` — apply the first via `settings.setMinimumInterval(minimumIntervalSeconds)` and pass the second through. Leave the returned tuple's six elements unchanged so no existing test's destructuring or behaviour changes.</action>
  <verify>Presence first: `grep -c 'await rateLimiter.setMinimumInterval(s.minimumIntervalSeconds)' src/Triggers/TriggerCoordinator.swift` prints 1 — this call did not exist at HEAD (the gap was zero callers). Then `grep -nE '\b(15|60)\b' src/Triggers/TriggerCoordinator.swift` finds no interval literal, and `SMOKE_DERIVED_DATA=build/dd-04-14 ./scripts/smoke.sh` exits 0.</verify>
  <falsify>Delete that `await rateLimiter.setMinimumInterval(...)` line from `applySettings`: the grep prints 0 and the check fails. Revert.</falsify>
  <done>One named call carries the stored interval to the enforcer on both the disk and the Settings path.</done>
</task>

<task type="auto">
  <name>Task 2: the launch value reaches the gate too</name>
  <files>src/App/GrokBotLocatorApp.swift</files>
  <action>Move `let triggerStore = UserDefaultsTriggerSettingsStore()` ABOVE line 87 and build the gate as `let rateLimiter = PingRateLimiter(minimumInterval: triggerStore.load().minimumIntervalSeconds)`. Reason to record in the comment: `TriggerCoordinator.start()` is a foreground-only touchpoint, so seeding at construction is what makes "the stored interval is in force from the first claim" true for the manual button and for any launch that has not run `start()` yet. Still exactly ONE `PingRateLimiter` in this file — constructing a second would split SC-04's window in silence. The limiter's init already clamps, so no floor check belongs here.</action>
  <verify>Presence first: `grep -c 'PingRateLimiter(minimumInterval: triggerStore.load().minimumIntervalSeconds)' src/App/GrokBotLocatorApp.swift` prints 1. Then `grep -c 'PingRateLimiter(' src/App/GrokBotLocatorApp.swift` prints 1 and `grep -c 'rateLimiter: rateLimiter' src/App/GrokBotLocatorApp.swift` prints 3 (PingModel, AutomaticPinger, TriggerCoordinator). Then smoke as in Task 1 exits 0.</verify>
  <falsify>Pass a fresh `PingRateLimiter()` to `TriggerCoordinator` instead of the local: the second grep prints 2 and the third prints 2 — both fail. Revert.</falsify>
  <done>One limiter, seeded from disk, shared by all three collaborators.</done>
</task>

<task type="auto">
  <name>Task 3: the two tests that fail without the wiring</name>
  <files>tests/SettingsModelTests.swift, tests/TriggerCoordinatorTests.swift</files>
  <action>Express every interval as `PingRateLimiter.defaultInterval` arithmetic — no literal 15 or 60. Both tests hold a REAL `PingRateLimiter` (default interval) and assert on `claim(at:)` with fixed dates.
In tests/SettingsModelTests.swift (the full chain, the UI's own path): `@Test func anIntervalChangedInSettingsReachesTheGate() async`. Build a real `TriggerCoordinator` from the internal fakes — `TriggerCoordinatorTests.FakeTriggerSource()`, `InMemoryGeofence()`, `TriggerCoordinatorTests.CountingPinger(scriptedResults: [])`, `TriggerCoordinatorTests.FakeFixProvider(result: .failure(KeychainFailure()))` (no trigger event is delivered, so no fix is ever requested), `TriggerCoordinatorTests.FakeSettingsStore(initial: .initial)`, the real limiter, `drain: {}`. `await coordinator.start()`, then `let model = SettingsModel(store: FakeCredentialStore(), triggers: coordinator)`, `await model.loadTriggers()`, `await model.setMinimumInterval(PingRateLimiter.defaultInterval * 2)` — the exact call SettingsView's Stepper makes. With `t = Date(timeIntervalSince1970: 1_700_000_000)`: `#expect(await limiter.claim(at: t) == .allowed)`, then `guard case .tooSoon = await limiter.claim(at: t.addingTimeInterval(PingRateLimiter.defaultInterval * 1.5))` else `Issue.record("the gate ignored the interval set in Settings")`. That second claim is ALLOWED at the default, so the test fails unless the value crossed over.
In tests/TriggerCoordinatorTests.swift (the launch path): `@Test func anIntervalOnDiskReachesTheGateAtStart() async`. `let limiter = PingRateLimiter()`; `Self.makeCoordinator(minimumIntervalSeconds: PingRateLimiter.defaultInterval * 2, rateLimiter: limiter)`; `await coordinator.start()` and nothing else — no Settings visit; same two claim assertions.</action>
  <verify>Presence first: `grep -c 'func anIntervalChangedInSettingsReachesTheGate' tests/SettingsModelTests.swift` prints 1 and `grep -c 'func anIntervalOnDiskReachesTheGateAtStart' tests/TriggerCoordinatorTests.swift` prints 1. Then run smoke as in Task 1 and require BOTH runner lines in build/dd-04-14.log: `grep -F 'Test anIntervalChangedInSettingsReachesTheGate() passed' build/dd-04-14.log` and `grep -F 'Test anIntervalOnDiskReachesTheGateAtStart() passed' build/dd-04-14.log` each match. Never assert on `** TEST SUCCEEDED **`, a suite name alone, or a test count.</verify>
  <falsify>Delete Task 1's `await rateLimiter.setMinimumInterval(s.minimumIntervalSeconds)` line: both runner lines are absent and the log shows both tests failed. Revert.</falsify>
  <done>Two tests that fail at the old wiring and pass at the new one — one for a value changed in Settings, one for a value already on disk.</done>
</task>
</tasks>
