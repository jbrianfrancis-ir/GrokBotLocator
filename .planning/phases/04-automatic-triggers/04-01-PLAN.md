---
phase: 04-automatic-triggers
plan: 01
wave: 1
depends_on: []
files_modified: [project.yml, scripts/smoke.sh]
autonomous: true
requirements: [REQ-06, REQ-07, REQ-08, REQ-10]
must_haves:
  truths:
    - "The BUILT app's Info.plist declares UIBackgroundModes containing both `location` and `fetch`, so the OS may wake or relaunch the app for a location event AND still run the BGAppRefreshTask drain"
    - "The BUILT app's Info.plist carries NSLocationAlwaysAndWhenInUseUsageDescription, so the Always prompt has a purpose string and does not silently no-op"
    - "The forbidden continuous-GPS call pattern (`startUpdatingLocation`, `allowsBackgroundLocationUpdates`, `CLBackgroundActivitySession`) still fails the smoke gate anywhere in src/, unconditionally — the plist key widened, the code ban did not"
    - "`requestAlwaysAuthorization` is permitted in exactly one named file and fails the gate anywhere else, and the exclusion is anchored on grep's own path prefix — a violation written on the same line as that filename is still caught"
    - "`CLBackgroundActivitySession` stays banned by an explicit ruling, not by omission: it is a When-In-Use convenience (RESEARCH Q3), not the `CLServiceSession` phase 04 may need, and the guard says so in its own failure message"
    - "The composition root can name `CLMonitorGeofence` and `UserDefaultsTriggerSettingsStore` without tripping a guard, while a real `CLMonitor(` or `UserDefaults` call beside either token ON THE SAME LINE still fails"
    - "MapKit is permitted in exactly `src/Triggers/MapKitTriggerLabelProvider.swift` and fails the gate everywhere else — both the import and any MapKit TYPE, because ARCHITECTURE's D-15 Forbidden entry bans more than the import"
    - "Every guard changed here has been probed by ESCAPE — a plausible violation added in a NEW place — not only by deleting the thing it protects, and every probe runs on a throwaway copy outside the repo"
  artifacts: [project.yml, scripts/smoke.sh]
  key_links:
    - "scripts/smoke.sh's background-identifier guard already reads the BUILT Info.plist; the new UIBackgroundModes check reads the same file the same way"
---

<objective>
Open the two doors phase 04 needs — the `location` background mode and an Always purpose string —
and move the guards so they permit exactly that and nothing more.
</objective>

<context>
.planning/ARCHITECTURE.md Forbidden — "Continuous background GPS (`startUpdatingLocation` +
`allowsBackgroundLocationUpdates`)". That bans a CALL PATTERN, not a plist key.
.planning/phases/04-automatic-triggers/RESEARCH.md Q4 — there is only one `location`
`UIBackgroundModes` string; Apple has no separate flag for wake-based vs continuous. Declaring it
is unavoidable for significant-change, visits and `CLMonitor` wake, and is NOT evidence of the
forbidden pattern. Say so in both files so a later audit does not trip over it.
.planning/LEARNINGS.md — "Probe a guard by ESCAPE, not by absence"; "A guard that filters whole
lines can be escaped on that line"; "Whole-file presence greps count comment text"; "Parallel
executors in ONE checkout cannot commit safely" (which is why the probes below never touch this
checkout — three wave-1 siblings are building against it while this runs).
scripts/smoke.sh — the existing location, UserDefaults and queue-store guards, and the
background-identifier guard that already parses the built Info.plist with PlistBuddy. Line 77 is
the UserDefaults guard's token-strip; it strips exactly one token today.
.planning/DECISIONS.md D-15 (2026-09-11) and .planning/ARCHITECTURE.md `## Forbidden` — MapKit is
authorized for reverse geocoding ONLY, and importing it anywhere but
`src/Triggers/MapKitTriggerLabelProvider.swift` is Forbidden. That is a confinement, not a ban, and
this plan is where it becomes enforceable.
Consumers this plan must not break: 04-04 writes `src/Triggers/MapKitTriggerLabelProvider.swift`,
04-07 writes `src/Triggers/LocationDelegateProxy.swift`,
04-08 writes `src/Triggers/GeofenceMonitor.swift`, 04-06 writes
`src/Settings/TriggerSettingsStore.swift`, and 04-13 writes `let geofence = CLMonitorGeofence()`
and `let triggerStore = UserDefaultsTriggerSettingsStore()` into `src/App/GrokBotLocatorApp.swift`.
Every one of those is a code line in a NON-exempt file, so comment-stripping does not cover them
and the type names must be stripped as allowed tokens.
Gate: SMOKE_DERIVED_DATA=build/dd-04-01 ./scripts/smoke.sh
In-task vs fan-in (.planning/LEARNINGS.md): every guard assertion below is checkable in-task and
is what this plan is accountable for — the guards run before `xcodegen`, so they are observable
even when a sibling's half-landed edit breaks the build. `smoke exits 0` as a whole is NOT reliably
observable mid-wave: per-plan `SMOKE_DERIVED_DATA` isolates the build LOG, not the SOURCES. Run it,
report verbatim what it said, and treat a BUILD failure traceable to a sibling as deferred to the
orchestrator's fan-in gate — a GUARD failure is always this plan's to fix.
</context>

<tasks>
<task type="auto">
  <name>Task 1: project.yml — background modes and the Always purpose string</name>
  <files>project.yml</files>
  <action>Under `targets.GrokBotLocator.info.properties`:
- Change `UIBackgroundModes: [fetch]` to `UIBackgroundModes: [location, fetch]`. Directly above it write a comment stating plainly: `location` here is the WAKE-based mode required for significant-change, visits and `CLMonitor` region events (REQ-06/07/08); it is the same string a continuous-GPS app would declare because iOS has only one, and ARCHITECTURE's Forbidden entry bans the `startUpdatingLocation` + `allowsBackgroundLocationUpdates` CALL PATTERN, which smoke.sh still rejects outright (see Task 2). `fetch` stays for `BGAppRefreshTask`. (project.yml is not under `src/`, so naming those identifiers in this comment is safe; inside `src/` it is not — see Task 2.)
- Add `NSLocationAlwaysAndWhenInUseUsageDescription` with a purpose string naming what Always buys and when it is asked for, e.g. "GrokBotLocator sends your location to your Grok webhook automatically when you move or arrive somewhere — only while the triggers you switched on in Settings are on." Do NOT add the iOS-10-deprecated `NSLocationAlwaysUsageDescription` (RESEARCH Q4).
- Leave `NSLocationWhenInUseUsageDescription` unchanged, and update its comment so it no longer says "Always arrives with phase 04's triggers" as future tense.
No credential, no bundle-id literal, no coordinate anywhere in this file (ARCHITECTURE Forbidden).</action>
  <verify>`SMOKE_DERIVED_DATA=build/dd-04-01 ./scripts/smoke.sh` exits 0, then `/usr/libexec/PlistBuddy -c "Print :NSLocationAlwaysAndWhenInUseUsageDescription" build/dd-04-01/Build/Products/Debug-iphonesimulator/GrokBotLocator.app/Info.plist` prints a non-empty string and `/usr/libexec/PlistBuddy -c "Print :UIBackgroundModes" build/dd-04-01/Build/Products/Debug-iphonesimulator/GrokBotLocator.app/Info.plist` prints a list containing both `location` and `fetch`.</verify>
  <falsify>Delete the `NSLocationAlwaysAndWhenInUseUsageDescription` line from project.yml, re-run: PlistBuddy exits non-zero with "Does Not Exist". Revert.</falsify>
  <done>The built app declares both background modes and both usage descriptions.</done>
</task>

<task type="auto">
  <name>Task 2: smoke.sh — narrow the guards instead of deleting them</name>
  <files>scripts/smoke.sh</files>
  <action>Rewrite the `location guard` block's banner to exactly:
`echo "==> location guard: continuous-GPS APIs banned outright; Always in LocationDelegateProxy only; CLLocationManager in 2 files; CLMonitor in GeofenceMonitor"`
and the UserDefaults guard's banner to exactly:
`echo "==> UserDefaults guard: the UserDefaults API confined to PingLabelStore.swift and TriggerSettingsStore.swift"`
Those two literal strings are what this task's verify greps for; keep them character-for-character.

Inside the location guard block, four checks:
1. **The unconditional ban.** `startUpdatingLocation|allowsBackgroundLocationUpdates|CLBackgroundActivitySession` matched anywhere in `src`, with NO file exemption and — deliberately — NO comment-strip. A guard with no exemption at all is the one shape nothing can escape, and these three are ARCHITECTURE's Forbidden continuous-GPS pattern. The cost is that no file under `src/` may name them even in prose; 04-07 is written to obey that, and it is the only plan that wanted to. Amend the failure message to say Always is now permitted in one named file while these three are permitted nowhere, and add a comment ruling explicitly on the third: **phase 04 does NOT need `CLBackgroundActivitySession` and the ban STAYS.** RESEARCH Q3 is explicit that it is a *When-In-Use* convenience — it keeps a when-in-use app alive in the background and "is not a substitute for Always authorization and does not itself enable the terminated-app relaunch behavior REQ-06/07/08 depend on". What phase 04 may need is `CLServiceSession` (RESEARCH Q2/Q5), a DIFFERENT type with a different spelling that this pattern does not match, so keeping the ban costs phase 04 nothing. Name both types in that comment so the next reader does not confuse them or read the surviving ban as an oversight.
2. **Always, in one file.** `requestAlwaysAuthorization` allowed ONLY in `src/Triggers/LocationDelegateProxy.swift`. Exclude that file with `grep -v "^src/Triggers/LocationDelegateProxy.swift:"` — anchored on grep's own `path:lineno:` prefix — and **never** with a filter that drops lines merely MENTIONING the filename. LEARNINGS records that exact hole: the old UserDefaults guard dropped whole lines containing an allowed token, so a real violation beside that token on the same line passed clean. A `^path:` anchor keys off which FILE grep reported the hit from, which no line's content can forge. Then strip comment-only lines (`grep -vE '^[^:]*:[0-9]+: *(///|//|\*)'`) before matching.
3. **CLLocationManager/CLLocationUpdate, in two files.** Widen the existing allow-list from `src/Core/Location/CoreLocationFixProvider.swift` to that file plus `src/Triggers/LocationDelegateProxy.swift`, same `^path:` anchors. Nothing else may name either.
4. **CLMonitor, in one file.** `CLMonitor` allowed only in `src/Triggers/GeofenceMonitor.swift`. **Strip the concrete type token first** — `sed 's/CLMonitorGeofence//g'` — because 04-13 writes `let geofence = CLMonitorGeofence()` in the composition root, a CODE line that comment-stripping does not reach and that the bare pattern matches. Strip the token, then match, exactly the shape the UserDefaults guard already uses for `UserDefaultsPingLabelStore`. Comment-strip as well, and use `^path:` anchors.

Add a FIFTH guard block, its own section with banner exactly:
`echo "==> MapKit guard: MapKit confined to MapKitTriggerLabelProvider.swift (D-15 authorizes reverse geocoding only)"`
Two checks, both permitting ONLY `src/Triggers/MapKitTriggerLabelProvider.swift`, both using the
same two protections as the Always arm above — `grep -v "^src/Triggers/MapKitTriggerLabelProvider.swift:"`
anchored on grep's own `path:lineno:` prefix (never a content filter), and a comment-only strip
(`grep -vE '^[^:]*:[0-9]+: *(///|//|\*)'`) so a doc comment naming MapKit in prose is not a hit:
 1. the import — `^[[:space:]]*(@[A-Za-z]+[[:space:]]+)*import[[:space:]]+MapKit[[:space:]]*$`, the
    same attribute-tolerant anchoring the existing `import Network` guard uses, since
    `@preconcurrency import Network` walked past the naive form once already (LEARNINGS);
 2. MapKit TYPES — `\bMK[A-Z][A-Za-z]*` — because the Forbidden entry bans map views, map tiles and
    MapKit types in the ping path, not merely the import line, and a type can reach a file through
    a re-export without an `import MapKit` of its own.
The failure message names D-15 and says the authorization is reverse geocoding only.

In the UserDefaults guard block: extend the exemption from one file to two — `src/Ping/PingLabelStore.swift` and `src/Settings/TriggerSettingsStore.swift` — and extend the token-strip at line 77 from one token to two: `sed -e 's/UserDefaultsPingLabelStore//g' -e 's/UserDefaultsTriggerSettingsStore//g'`. Both are needed: 04-13 writes `let triggerStore = UserDefaultsTriggerSettingsStore()` into `GrokBotLocatorApp.swift`, which is NOT an exempt file, and that type name contains the string `UserDefaults`. Write the reason into the comment: a per-trigger on/off switch and a minimum-interval number are neither a credential nor a coordinate, so ARCHITECTURE's Forbidden entry does not reach them; the guard exists to keep the sender key, the webhook URL and coordinates out of UserDefaults, and that is unchanged.

The named exempt files do not exist yet; `grep -v "^path:"` on an absent path is harmless and each guard is correct the moment its file lands.</action>
  <verify>All three are presence checks on strings that do not exist at HEAD: `grep -c '==> location guard: continuous-GPS APIs banned outright; Always in LocationDelegateProxy only; CLLocationManager in 2 files; CLMonitor in GeofenceMonitor' scripts/smoke.sh` prints 1; `grep -c '==> UserDefaults guard: the UserDefaults API confined to PingLabelStore.swift and TriggerSettingsStore.swift' scripts/smoke.sh` prints 1; `grep -c 's/CLMonitorGeofence//g' scripts/smoke.sh` prints 1 and `grep -c 's/UserDefaultsTriggerSettingsStore//g' scripts/smoke.sh` prints 1; and `grep -c '==> MapKit guard: MapKit confined to MapKitTriggerLabelProvider.swift (D-15 authorizes reverse geocoding only)' scripts/smoke.sh` prints 1. Then `SMOKE_DERIVED_DATA=build/dd-04-01 ./scripts/smoke.sh` exits 0 and its output contains all three banner strings above.</verify>
  <falsify>Delete the `-e 's/CLMonitorGeofence//g'` argument from the CLMonitor guard's sed: `grep -c 's/CLMonitorGeofence//g'` prints 0 and the check fails (and probe (i) in Task 3 would then fail too). Revert.</falsify>
  <done>Always is permitted in one file; the three continuous-GPS APIs are permitted nowhere; both new type names are strippable tokens.</done>
</task>

<task type="auto">
  <name>Task 3: the built-plist check, and thirteen escape probes on a throwaway copy</name>
  <files>scripts/smoke.sh</files>
  <action>First add one more check, placed beside the existing background-identifier guard so it reuses `$APP_PLIST` (already derived from `$DERIVED_DATA`, never a literal). Banner exactly:
`echo "==> background-modes guard: the BUILT Info.plist declares UIBackgroundModes location and fetch"`
Read `UIBackgroundModes` from the BUILT Info.plist with PlistBuddy and fail unless the printed value contains BOTH `location` and `fetch`; the message names which one is missing. This is the check that proves Task 1 survived into the product rather than only into project.yml.

Then PROBE, by escape. **Every probe runs on a throwaway copy OUTSIDE this repo, never here** — `cp -R "$PWD" "${TMPDIR:-/tmp}"/smoke-probe-04-01`, work in that copy, delete it at the end. Three wave-1 siblings are running whole-project builds against this checkout while this plan executes, and an add-then-revert probe puts a deliberately broken file in front of them and a stray `src/Triggers/Probe.swift` into a shared git index (.planning/LEARNINGS.md). There is no add-then-revert alternative; do not use one.

A probe is judged on the GUARD message, not on the exit code alone — the guards run before `xcodegen`, so a probe file that does not compile would exit 1 for the wrong reason. Ten probes must FAIL, each with its named guard message in the output:
 (a) a new file `src/Triggers/Probe.swift` containing `CLLocationManager()` → "location guard failed"; the widened confinement must still reject a third file;
 (b) `manager.requestAlwaysAuthorization()` added to `src/Core/Location/CoreLocationFixProvider.swift` → "location guard failed";
 (c) in `src/App/RootView.swift`, the SINGLE line `// see src/Triggers/LocationDelegateProxy.swift` followed on that same line by `; manager.requestAlwaysAuthorization()` → "location guard failed". This is the line-escape shape LEARNINGS documents. If it PASSES, the exclusion was written as a content filter instead of a `^path:` anchor — fix the guard;
 (d) `let d = UserDefaults.standard` added to `src/Queue/PingQueueStore.swift` → "UserDefaults guard failed"; the second exemption must not have opened the guard generally;
 (e) in `src/App/GrokBotLocatorApp.swift`, the SINGLE line `let g = CLMonitorGeofence(); _ = await CLMonitor("x")` → "location guard failed". The token-strip must not hide a real call sitting beside the allowed token;
 (f) in `src/App/GrokBotLocatorApp.swift`, the SINGLE line `let s = UserDefaultsTriggerSettingsStore(); UserDefaults.standard.set(1, forKey: "x")` → "UserDefaults guard failed". Same shape, the other guard;
 (g) `session = CLBackgroundActivitySession()` added to `src/Triggers/LocationDelegateProxy.swift` → "location guard failed"; the ruling in Task 2 says the ban survives even inside the one file Always is permitted in;
 (h) `CLMonitor(` named in `src/App/RootView.swift` → "location guard failed";
 (k) `import MapKit` added to `src/Triggers/TriggerLabel.swift` → "MapKit guard failed"; the confinement must reject a sibling in the same folder;
 (l) in `src/App/GrokBotLocatorApp.swift`, the SINGLE line `// see src/Triggers/MapKitTriggerLabelProvider.swift` followed on that same line by `; let v = MKMapView()` → "MapKit guard failed". Both the line-escape shape AND the type arm at once: a MapKit type with no `import MapKit` of its own must still be caught;
 (m) `let r = MKReverseGeocodingRequest(location: loc)` added to `src/Triggers/AutomaticPinger.swift` → "MapKit guard failed"; D-15 bans MapKit types in the ping path, and this is the ping path.
Three probes must PASS the GUARDS — these are the allow-side controls for issues the guards were widened for, and they are judged ONLY on the absence of a guard message, because the types do not exist yet and the BUILD will fail:
 (i) `let g = CLMonitorGeofence()` alone on a line in `src/App/GrokBotLocatorApp.swift` → output contains neither "location guard failed" nor "UserDefaults guard failed";
 (j) `let s = UserDefaultsTriggerSettingsStore()` alone on a line in `src/App/GrokBotLocatorApp.swift` → same;
 (n) a new file `src/Triggers/MapKitTriggerLabelProvider.swift` containing `import MapKit` and `MKReverseGeocodingRequest` → output contains no "MapKit guard failed". This is the allow-side control for the whole D-15 confinement; without it the guard could be rejecting everything and the must-FAIL probes would still look right.
Record all thirteen results in the SUMMARY. If a must-FAIL probe passes, or a must-PASS probe trips a guard, the guard is wrong — fix the guard, do not record the probe as done.</action>
  <verify>`grep -c '==> background-modes guard: the BUILT Info.plist declares UIBackgroundModes location and fetch' scripts/smoke.sh` prints 1 (a string absent at HEAD). Then, on the clean tree, `SMOKE_DERIVED_DATA=build/dd-04-01 ./scripts/smoke.sh` exits 0 and prints that banner. Then, in the throwaway copy only, each of the ten must-FAIL probes makes smoke exit 1 with its named guard message, and each of the three must-PASS probes produces no guard message (run them one at a time, restoring the copy between).</verify>
  <falsify>Remove the `location` entry from `UIBackgroundModes` in project.yml: the new built-plist check fails with exit 1 and names `location` as missing. Revert.</falsify>
  <done>Thirteen probes observed — ten failing on their named guard, three passing the guards — all on a copy outside the repo; the clean tree passes.</done>
</task>
</tasks>
