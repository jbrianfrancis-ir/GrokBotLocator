---
phase: 04-automatic-triggers
plan: 15
wave: 8
depends_on: [04-08, 04-11, 04-13, 04-14]
files_modified: [src/Triggers/LastPingStore.swift, src/Triggers/TriggerCoordinator.swift, src/Triggers/GeofenceMonitor.swift, src/App/GrokBotLocatorApp.swift, scripts/smoke.sh, tests/LastPingStoreTests.swift, tests/TriggerCoordinatorTests.swift, tests/SettingsModelTests.swift, .planning/phases/04-automatic-triggers/RESEARCH.md]
autonomous: true
requirements: [REQ-08]
must_haves:
  truths:
    - "A cold start with ONLY the geofence trigger enabled recovers the last-ping reference: after `start()`, `currentReference()` is the last ping's coordinate. At HEAD it is nil, because the only recovery sits inside `runSignificantChange`, which this configuration never runs"
    - "That recovery works even when no region survived the relaunch: with `currentCentre()` nil and a coordinate in the last-ping store, `start()` recovers the reference AND registers the region — REQ-08 arms in the geofence-only configuration whatever CLMonitor did"
    - "Recovery sits on the arming path both `start()` and `update(_:)` reach, and is defined ONCE — `runSignificantChange` and `applySettings` call the same helper, not two copies that can drift"
    - "D-16 condition 1 — exactly one coordinate, overwritten in place: after two saves the file's bytes are a single JSON object (no `[`) and `load()` returns the second coordinate. It is a point, never a list"
    - "D-16 condition 2 — the last-ping file is excluded from backups, read back off the real file via `.isExcludedFromBackupKey`"
    - "D-16 condition 3 — the file is written with `.completeFileProtectionUntilFirstUserAuthentication`, deliberately weaker than the queue's, and the code states WHY: a geofence exit fires while the phone is locked in a pocket, and under complete protection that write would fail on exactly that wake"
    - "D-16 condition 4 — disabling every trigger deletes the coordinate: after a ping and then `update(.initial)`, the store is empty and the in-memory reference is nil"
    - "There is no THIRD store: the only file-writing APIs in `src/` live in `PingQueueStore.swift` and `LastPingStore.swift`, enforced by smoke's widened guard, which still catches a writer added in any other file"
    - "`TriggerCoordinator` itself still does no persistence — it names no `UserDefaults`, `FileManager`, `JSONEncoder` or `.write(`. It decides; something else stores, behind an injected protocol"
    - "REQ-08's proven behaviour is untouched: one fixed `conditionIdentifier`, 150 m, exit-only — and `start()` does not re-register over a region that is already there"
    - "RESEARCH.md's UNVERIFIED entry for `CLMonitor(name:)` cross-launch persistence records a MEASURED result with the commands that produced it, replacing the inference from the `async` init signature"
  backstop_truths: []
  artifacts: [src/Triggers/LastPingStore.swift, tests/LastPingStoreTests.swift, src/Triggers/TriggerCoordinator.swift, tests/TriggerCoordinatorTests.swift, .planning/phases/04-automatic-triggers/RESEARCH.md]
  key_links:
    - "TriggerCoordinator.pingAndAdvance calls lastPing.save(coordinate) — the write that makes the reference survive a relaunch"
    - "TriggerCoordinator.applySettings calls recoverReferenceIfNeeded() in its geofence branch, and lastPing.clear() when no trigger is enabled (D-16 condition 4)"
    - "recoverReferenceIfNeeded() reads the last-ping store first and falls back to geofence.currentCentre() — the two durable records, in that order"
    - "GrokBotLocatorApp builds FileLastPingStore.applicationSupport() and injects it into TriggerCoordinator; nil when Application Support is unavailable, the same no-op fallback the queue already uses"
---

<objective>
Close VERIFICATION's REQ-08 gap: with only the geofence enabled, a cold relaunch can never arm —
`reference` is nil and nothing outside the disabled significant-change handler recovers it. D-16
authorizes the durable last-ping store that makes recovery possible when no region survived.
</objective>

<context>
.planning/ARCHITECTURE.md — `## Architecture & patterns`, the D-16 bullet: the second sanctioned
store and its FOUR binding conditions; `## Forbidden` now names two sanctioned stores. Read it, do
not work from this plan's paraphrase. .planning/DECISIONS.md D-16 for the reasoning.
.planning/phases/04-automatic-triggers/VERIFICATION.md — the gap and REQ-08's NOT REPRODUCED check.
RESEARCH.md Q2 + `## Unverified` — `CLMonitor(name:)` persistence is INFERRED, never Apple-quoted;
Task 1 settles it. D-16 does not settle it: if CLMonitor persists the file is belt-and-braces, if
not it is load-bearing.
src/Queue/PingQueueStore.swift:118-245 `FilePingQueueStore` — the precedent to copy: injectable
directory, `applicationSupport()`, protections re-asserted on EVERY write (an atomic replace
recreates the file and does not inherit resource values). tests/PingQueueStoreTests.swift:64
`theQueueFileIsExcludedFromBackup` — the precedent for reading protection off the real file.
scripts/smoke.sh:167 queue-store guard, :207 queue-protection guard — both must learn about the
second store. src/Triggers/TriggerCoordinator.swift:95 `applySettings`, :121 the register
condition, :187-192 the only recovery, :235 `pingAndAdvance`. src/Triggers/GeofenceMonitor.swift —
`currentCentre()`. tests/GeofenceMonitorTests.swift `InMemoryGeofence` (internal — reuse it).
Do NOT change the 150 m radius, the fixed `conditionIdentifier`, or exit-only delivery. Do not
touch the three open backstops — 04-05's `hydrate()` copying queue entries into `PingHistoryLog`
is STILL open: D-16 authorizes a last-ping store, it does not ratify that copy.
LEARNINGS that bind here: a fake that returns without suspending cannot test an actor (yield before
mutating); a whole-file grep counts comment text; every guard change needs an ESCAPE probe — a
plausible violation added in a NEW place — not a deletion probe. And: "a bound that depends on a
vendor API honouring `cancel()` is not a bound" — arming that depends on `CLMonitor` persisting is
the same sentence, which is why the durable file is built unconditionally and a surviving region is
treated as a bonus, never as the mechanism. Nothing sleeps in a test.
Gate: `SMOKE_DERIVED_DATA=build/dd-04-15 ./scripts/smoke.sh 2>&1 | tee build/dd-04-15.log`.
</context>

<tasks>
<task type="auto">
  <name>Task 1: settle CLMonitor cross-launch persistence by measurement</name>
  <files>.planning/phases/04-automatic-triggers/RESEARCH.md</files>
  <action>Build a THROWAWAY probe app in the scratchpad — outside this repo, never committed, so its
bundle id never reaches `src/`, `project.yml` or `scripts/`. XcodeGen 2.46.0 project, iOS 26
simulator, one SwiftUI file whose `.task` runs:
`let m = await CLMonitor("CLMonitorPersistenceProbe")`; append `launch=\(await m.identifiers.count)`
to `probe.log` in its Documents directory; if empty, `await m.add(CLMonitor.CircularGeographicCondition(center: .init(latitude: 40.0559, longitude: 17.9925), radius: 150), identifier: "probe-region")`
and append `after-add=\(await m.identifiers.count)`; then `for try await e in await m.events { append("event=\(e.state)") }`.
Counts and identifiers only — never a coordinate — in anything written or printed.
UDID from `scripts/simulator-udid.sh`. `xcodebuild ... -destination "id=$UDID" CODE_SIGNING_ALLOWED=NO build`,
`xcrun simctl install`, `xcrun simctl privacy "$UDID" grant location-always <probe id>`, launch,
then `xcrun simctl terminate "$UDID" <probe id>` and launch AGAIN — that second launch is the cold
relaunch under test. Read results from the host with
`cat "$(xcrun simctl get_app_container "$UDID" <probe id> data)/Documents/probe.log"`.
Second observation, same harness, same run: drive
`xcrun simctl location "$UDID" start --speed=20 --distance=50 scripts/gpx/req06-start.gpx scripts/gpx/req08-hop1.gpx`
(interpolated waypoints — a teleport produces no movement for the OS to notice) and record whether
any `event=` line appears. That distinguishes VERIFICATION's cause (a) from cause (b).
Then edit RESEARCH.md: add `## Q2 addendum — CLMonitor persistence, measured` carrying the date, the
exact commands, the verbatim `launch=` / `after-add=` / `event=` lines, and the verdict; and rewrite
the `## Unverified` bullet for `CLMonitor(name:)`'s disk persistence so its text begins
`settled by measurement — see Q2 addendum` and then states the measured result instead of the
inference. If a step genuinely could not run, write `PROBE INCONCLUSIVE — step: <the step> — <the
error>` on one line under the addendum heading and say so in the `## Unverified` bullet too:
"could not check" is not "pass", and it is not a licence to invent a result either.
Whatever the verdict, Tasks 2–4 proceed unchanged: D-16 authorizes the durable file either way.</action>
  <verify>Presence first, on text only this task creates (RESEARCH.md path throughout): `grep -c '^## Q2 addendum — CLMonitor persistence, measured' …/RESEARCH.md` prints 1. Then EITHER the measured form — `grep -cE 'launch=[0-9]' …/RESEARCH.md` and `grep -cF 'after-add=' …/RESEARCH.md` each print at least 1 (the probe's own lines, pasted verbatim) — OR, only if the probe genuinely could not run, the inconclusive form: `grep -cF 'PROBE INCONCLUSIVE — step:' …/RESEARCH.md` prints 1, followed on that same line by which step failed and the error it produced. Never both, and never neither: an absent probe is recorded as inconclusive, never as a pass and never as invented data. Either way `grep -cF 'settled by measurement — see Q2 addendum' …/RESEARCH.md` prints 1 — the `## Unverified` bullet now cites this addendum rather than the inference.</verify>
  <falsify>Delete the `## Q2 addendum — CLMonitor persistence, measured` heading line: the first grep prints 0 and the check fails. Revert.</falsify>
  <done>The persistence question is answered by a run, not an assumption, and RESEARCH.md says so — including whether CLMonitor events reach the simulator at all.</done>
</task>

<task type="auto">
  <name>Task 2: the last-ping store (D-16's four conditions) and the guards that hold it</name>
  <files>src/Triggers/LastPingStore.swift, tests/LastPingStoreTests.swift, scripts/smoke.sh</files>
  <action>New `src/Triggers/LastPingStore.swift`. Header doc cites D-16 and states all four conditions
plus the protection-class trade IN THE CODE (a geofence exit fires while the phone is locked in a
pocket; under `.completeFileProtectionUnlessOpen` that write fails on exactly that wake and the
region never re-registers — phase 03 shipped the inverse, PingQueueStore.swift:67,173).
`protocol LastPingStoring: Sendable { func load() async -> TriggerCoordinate?; func save(_ coordinate: TriggerCoordinate) async; func clear() async }`
— all non-throwing: a failed reference write must not fail a ping that already succeeded, and a read
before first unlock returns nil rather than throwing into the arming path.
`actor FileLastPingStore: LastPingStoring`, modelled on `FilePingQueueStore`: `init(directory: URL)`
and `static func applicationSupport() throws -> FileLastPingStore`, file name `LastPing.json`.
Encode a `private struct StoredCoordinate: Codable { let latitude: Double; let longitude: Double }`
— do NOT add `Codable` to `TriggerCoordinate` in DisplacementGate.swift; this store owns its own
wire shape. Encode the struct, never an array or a dictionary keyed by anything: one object is the
whole file, which is what makes condition 1 true by construction. Pin the protection as a named
seam rather than an inline literal, so the fallback assertion below has a real symbol to reference:
`static let writeOptions: Data.WritingOptions = [.atomic, .completeFileProtectionUntilFirstUserAuthentication]`
(the `Data.WritingOptions` spelling — note `UntilFirst` — of `FileProtectionType.completeUntilFirstUserAuthentication`;
the two types spell it differently on purpose, do not unify them). `save` writes
`try data.write(to: fileURL, options: Self.writeOptions)` and then re-asserts `isExcludedFromBackup` via `URLResourceValues` — both on EVERY write, for the
same reason the queue does it. `clear()` removes the file if it exists. Every error is swallowed
inside this actor; nothing propagates.
tests/LastPingStoreTests.swift, against a throwaway directory (copy `throwawayDirectory()` from
tests/PingQueueStoreTests.swift): `loadOnAFreshStoreIsNil`;
`savingTwiceOverwritesAndNeverAppends` — save A then B, `load()` is B, and the raw bytes read back
with `Data(contentsOf:)` as UTF-8 start with `{` and contain no `[`;
`theLastPingFileIsExcludedFromBackup` — `.isExcludedFromBackupKey` off the real file, as
PingQueueStoreTests.swift:64 does; `theLastPingFileCarriesTheStatedProtectionClass` — read
`FileManager.default.attributesOfItem(atPath:)[.protectionKey]` and expect
`FileProtectionType.completeUntilFirstUserAuthentication`. RUN THAT ONE FIRST: if the simulator
reports nil for the queue's own file too (a control the same test can write), then the runtime
attribute is not observable there — in that case assert
`FileLastPingStore.writeOptions.contains(.completeFileProtectionUntilFirstUserAuthentication)`
instead, say in the test's doc comment that the runtime class is unreadable on the simulator, and
add the on-device reading to Task 4's human check. Do not leave it asserting nothing;
`clearRemovesTheFile` —
`FileManager.default.fileExists(atPath:)` is false after `clear()`.
Also add `actor InMemoryLastPingStore: LastPingStoring` here, `internal` so Task 3 reuses it, with
`await Task.yield()` before every mutation (LEARNINGS: a fake that returns instantly cannot test an
actor).
scripts/smoke.sh: at the queue-store guard (:167) add `LAST_PING_STORE="src/Triggers/LastPingStore.swift"`
and a second `| grep -v "^${LAST_PING_STORE}:"` — anchored on grep's own `path:lineno:` prefix,
never a filter on lines mentioning the filename — and update the echo and the failure message to
name TWO sanctioned stores (D-12 and D-16). Then add a `==> last-ping-protection guard` block
mirroring :207: on NON-COMMENT lines of `$LAST_PING_STORE`, both
`completeFileProtectionUntilFirstUserAuthentication` and `isExcludedFromBackup` must appear, each with
its own failure message naming the D-16 condition that would be voided.
ESCAPE-probe BOTH guards separately, per LEARNINGS — a guard nobody has seen fail is not
established to fail. (a) The widened queue-store guard: add a `FileManager.default` call on a
non-comment line of a THIRD file, confirm smoke fails naming that file, revert. (b) The new
last-ping-protection guard, whose documented failure mode is counting comment text (phase 01: a doc
comment pushed a `grep -c` from 1 to 5): move the `isExcludedFromBackup` token out of code and onto
a comment-only line in `LastPingStore.swift`, confirm smoke fails naming the voided D-16 condition
rather than passing on the comment, revert.</action>
  <verify>Presence first, on symbols absent at HEAD: `grep -c 'actor FileLastPingStore' src/Triggers/LastPingStore.swift` prints 1 and `grep -c 'last-ping-protection guard' scripts/smoke.sh` prints 1. **The seam must be USED, not merely declared** — `grep -cF 'options: Self.writeOptions' src/Triggers/LastPingStore.swift` prints 1, and that line is not a comment; without it every other check below is satisfied by the `static let` declaration alone even if `save` passed a different literal, which is a verify that cannot fail for the reason it exists. Then the D-16 conditions, on non-comment lines: `grep -nE 'completeFileProtectionUntilFirstUserAuthentication|isExcludedFromBackup' src/Triggers/LastPingStore.swift | grep -vE '^[0-9]+: *(///|//|\*)'` shows both. Then run the gate and require all five runner lines in build/dd-04-15.log: `grep -F 'Test savingTwiceOverwritesAndNeverAppends() passed'`, `... theLastPingFileIsExcludedFromBackup() passed`, `... theLastPingFileCarriesTheStatedProtectionClass() passed`, `... clearRemovesTheFile() passed`, `... loadOnAFreshStoreIsNil() passed` each match, and smoke exits 0. Never assert on `** TEST SUCCEEDED **`, a suite name, or a test count.</verify>
  <falsify>The escape probe IS the falsification: add `_ = FileManager.default.temporaryDirectory` on a non-comment line of src/Triggers/TriggerCoordinator.swift — the widened queue-store guard must still fail, naming that file, so smoke exits non-zero and the verify fails. Revert. (A guard that exempts the new store by filename rather than by grep's path prefix passes this probe too, which is why the exemption is written as `^${LAST_PING_STORE}:`.)</falsify>
  <done>A second sanctioned store that satisfies D-16's four conditions, each pinned by a test that reads the real file, and guards that admit exactly two writers and no third.</done>
</task>

<task type="auto">
  <name>Task 3: recover the reference on the arming path, and the tests that fail without it</name>
  <files>src/Triggers/TriggerCoordinator.swift, src/App/GrokBotLocatorApp.swift, tests/TriggerCoordinatorTests.swift, tests/SettingsModelTests.swift</files>
  <action>`TriggerCoordinator.init` gains `lastPing: (any LastPingStoring)?` after `settingsStore`, with
NO default — a default is how a call site silently reacquires this gap. Optional for one reason
only: Application Support can be unavailable, the same case that already makes `queueCoordinator`
nil in the composition root; a nil store is a no-op, not a second in-memory conformer.
Add one helper beside `pingAndAdvance`:
`@discardableResult private func recoverReferenceIfNeeded() async -> TriggerCoordinate? { let centre = await geofence.currentCentre(); if reference == nil { reference = (await lastPing?.load() ?? nil) ?? centre }; return centre }`
— the DOUBLE unwrap is required and must not be "simplified". `lastPing` is optional and `load()`
returns an optional, so `await lastPing?.load()` is `TriggerCoordinate??`; the naive
`await lastPing?.load() ?? centre` unwraps only the OUTER optional, so a store that exists but is
EMPTY yields `.some(nil)` and returns that inner nil — `centre` is never reached, and the decisive
test below (which seeds only the geofence) would fail. Put that reason in a code comment.
Doc the helper as the ONE place a reference is recovered, reading the two durable records in order:
this app's own last-ping file (D-16), then the registered region. Replace the inline recovery at
:186-192 of `runSignificantChange` with
`await recoverReferenceIfNeeded()` so exactly one definition exists.
Rewrite `applySettings`'s geofence branch to `let centre = await recoverReferenceIfNeeded()` then
`if centre == nil, let reference { await geofence.register(at: reference) }` — the same register
condition as today, reading the centre once, with recovery ABOVE it. That pairing is what arms a
geofence-only cold start when no region survived: the file supplies the reference, and the nil
centre is what triggers the re-registration. The `else` branch (`unregister()`) is unchanged.
Comment the gap being closed: a geofence-only configuration never runs `runSignificantChange`.
At the END of `applySettings` add D-16 condition 4: `if !s.anyTriggerEnabled { await lastPing?.clear(); reference = nil }` — nothing is watching, so nothing needs the position, in memory or on disk.
In `pingAndAdvance`, directly after `reference = coordinate`, add `await lastPing?.save(coordinate)`
— overwrite in place, above the existing geofence re-registration, so only a real `.pinged` result
ever moves it (unchanged).
GrokBotLocatorApp.swift: build `let lastPing = try? FileLastPingStore.applicationSupport()` beside
the other trigger collaborators and pass `lastPing: lastPing` into `TriggerCoordinator`. Comment it
as the D-16 store, nil-when-unavailable, exactly like the queue.
Correct the two stale premises IN THIS FILE, both of which D-16 now contradicts (Task 4 does the
same for `GeofenceMonitor.swift`; a comment that contradicts the law is how the next reader is
misled): the `reference` doc at :31-34 says "this app keeps no second coordinate file of its own --
D-12 reserves that role for the offline queue alone", and the rehydration comment at :188-190 says
a fresh process recovers from the region "rather than this app keeping a second coordinate file".
Rewrite both to name the two durable records and the order they are read in, citing D-16.
tests/SettingsModelTests.swift:695 constructs a `TriggerCoordinator` directly — add
`lastPing: InMemoryLastPingStore()` to that call so it still compiles; change nothing else there.
tests/TriggerCoordinatorTests.swift: give `makeCoordinator` a `lastPing: any LastPingStoring = InMemoryLastPingStore()`
parameter and pass it through. Leave the returned 6-tuple UNCHANGED so no existing test's
destructuring breaks. Add three tests:
`aColdStartWithOnlyTheGeofenceEnabledRecoversTheReference` — `makeCoordinator(geofenceEnabled: true)`
(the other two default off), `await geofence.register(at: lastPingCoordinate)` to stand in for a
region a PREVIOUS process left behind, `await coordinator.start()` and nothing else; assert
`currentReference()` is that coordinate and `geofence.registrations.count == 1` (start() must not
re-register over a region already there).
`aColdStartWithNoRegionRecoversFromTheLastPingFileAndArms` — the D-16 path: a store pre-seeded via
`await store.save(coordinate)`, the geofence left EMPTY, `start()`; assert `currentReference()` is
that coordinate AND `geofence.currentCentre()` is now that coordinate, i.e. the region was
registered from the file.
`disablingEveryTriggerDeletesTheLastPingCoordinate` — drive one successful ping through an enabled
trigger, assert the store holds that coordinate (this is the `save` on the ping path), then
`await coordinator.update(.initial)`; assert `await store.load() == nil` and
`await coordinator.currentReference() == nil`.
Run all three BEFORE editing the coordinator and record that they fail; the wiring is what makes
them pass.</action>
  <verify>Presence first, on symbols absent at HEAD: `grep -c 'private func recoverReferenceIfNeeded' src/Triggers/TriggerCoordinator.swift` prints 1, `grep -c 'await lastPing?.save(coordinate)' src/Triggers/TriggerCoordinator.swift` prints 1, `grep -c 'FileLastPingStore.applicationSupport' src/App/GrokBotLocatorApp.swift` prints 1. Then the coordinator still persists nothing itself: `grep -cE 'UserDefaults|FileManager|JSONEncoder|\.write\(' src/Triggers/TriggerCoordinator.swift` prints 0. Then the stale premise is gone: `grep -cF 'keeps no second coordinate file of its own' src/Triggers/TriggerCoordinator.swift` prints 0 — it prints 1 at HEAD, so this is not an absence that passes before the work. Then run the gate and require all three runner lines in build/dd-04-15.log: `grep -F 'Test aColdStartWithOnlyTheGeofenceEnabledRecoversTheReference() passed'`, `... aColdStartWithNoRegionRecoversFromTheLastPingFileAndArms() passed`, `... disablingEveryTriggerDeletesTheLastPingCoordinate() passed` each match, and smoke exits 0 (its widened queue-store guard must not trip on the coordinator, which writes no file itself).</verify>
  <falsify>Delete the `await recoverReferenceIfNeeded()` call from `applySettings`'s geofence branch, leaving the helper and `runSignificantChange`'s call in place — that is exactly HEAD's behaviour: the first two runner lines are absent and the log shows both tests failed with `currentReference()` nil. Revert.</falsify>
  <done>A geofence-only cold start recovers a reference and arms the region whether or not CLMonitor persisted anything, proven by tests that fail at HEAD.</done>
</task>

<task type="auto">
  <name>Task 4: correct REQ-08's reproduction steps and re-run the acceptance check</name>
  <files>src/Triggers/GeofenceMonitor.swift</files>
  <action>Two comment-only edits, no code change. (1) The header claims this file's region is the
durable record "rather than this app keeping a second coordinate file" — that premise is superseded
by D-16. Rewrite those lines to say the region is ONE of two durable records, the other being
`LastPingStore.swift` (D-16, 2026-09-11), and that `currentCentre()` remains the region's own
read-back. (2) Rewrite the `On-device verification` block at the foot: it says "Debug ▸ Simulate
Location (or a GPX route)", which VERIFICATION proved does not work — a teleport produces no
movement for the OS to notice. Replace steps 3–5 with the machine-driven procedure that did work
for REQ-06: `UDID=$(scripts/simulator-udid.sh)`, grant Always via `xcrun simctl privacy`, settle at
`scripts/gpx/req06-start.gpx`, then
`xcrun simctl location "$UDID" start --speed=20 --distance=50 scripts/gpx/req06-start.gpx scripts/gpx/req08-hop1.gpx`
for the first exit and `... req08-hop1.gpx scripts/gpx/req08-hop2.gpx` for the second. Add a step 0:
switch OFF significant-change and visits so any ping must be a geofence exit, and terminate then
relaunch the app before driving — that is the cold-relaunch case this plan fixes. No banned token
(`CLLocationManager`, `startUpdatingLocation`, `allowsBackgroundLocationUpdates`,
`CLBackgroundActivitySession`) in the new prose: LEARNINGS records a comment alone failing smoke's
location guard.</action>
  <verify>Presence first: `grep -c 'simctl location' src/Triggers/GeofenceMonitor.swift` prints at least 1 (absent at HEAD) and `grep -c 'Debug ▸ Simulate Location' src/Triggers/GeofenceMonitor.swift` prints 0. Then `SMOKE_DERIVED_DATA=build/dd-04-15 ./scripts/smoke.sh` exits 0 — the location guard reads comments too. The acceptance clause itself is a <human-check>, exempt from falsification: <human-check>Geofence trigger ON, significant-change and visits OFF, Always granted. Send one manual ping, terminate the app, relaunch, then drive req08-hop1: exactly ONE new row appears, marked as a geofence exit. Drive req08-hop2 from there: a SECOND row appears, proving re-registration. If no row appears, report whether Task 1's probe found `event=` lines — no events in the simulator is cause (a) and needs a device; events but no row is a live defect. Also report, if Task 2 found the protection class unreadable on the simulator, what `attributesOfItem(atPath:)[.protectionKey]` returns for LastPing.json on a real device.</human-check></verify>
  <falsify>Delete the `xcrun simctl location` line from the comment block: the first grep prints 0 and the check fails. Revert.</falsify>
  <done>The file's own premise and repro steps match what is now law and what is known to work, and REQ-08's acceptance clause is queued for the end-of-phase human batch with both outcomes distinguishable.</done>
</task>
</tasks>
