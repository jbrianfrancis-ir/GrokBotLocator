---
phase: 01-foundation-design-system
plan: 02
wave: 2
depends_on: [01-01]
files_modified: [scripts/simulator-udid.sh, src/App/GrokBotLocatorApp.swift, src/App/RootView.swift, tests/SmokeTests.swift]
autonomous: true
requirements: [REQ-12]
must_haves:
  truths:
    - "simulator-udid.sh prints one udid for an available iOS 26 iPhone, non-zero if none"
    - "xcodebuild test builds the app and runs the bundle against its host with a non-zero test count"
    - "RootView renders a plain unstyled Text; 01-03 gives it the type token once dsFont exists"
  artifacts: [scripts/simulator-udid.sh, src/App/RootView.swift, tests/SmokeTests.swift]
  key_links:
    - "GrokBotLocatorApp presents RootView; the app links and builds"
    - "SmokeTests imports the app module, proving test-host wiring"
---

<objective>
The first buildable slice: a simulator resolver, the `@main` entry point the target needs
before anything compiles, and a test proving host wiring.
</objective>

<context>
.planning/DESIGN.md and .planning/ARCHITECTURE.md are law. From 01-01: project.yml and
DSMetrics — DSTypography is 01-03's and arrives after this plan. Signing.xcconfig exists
locally. An iOS 26 runtime with several iPhone models is installed; never hardcode a name.
01-04 wraps this into ./scripts/smoke.sh.
</context>

<tasks>
<task type="auto">
  <name>Task 1: Simulator resolver</name>
  <files>scripts/simulator-udid.sh</files>
  <action>Executable bash, `set -euo pipefail`. Prints exactly one udid on stdout. Honour $SIMULATOR_NAME; else parse `xcrun simctl list devices available -j` with python3 and take the first available iPhone whose runtime id contains `iOS-26`. Nothing matching: say so on stderr, exit 1. Never hardcode a device name.</action>
  <verify>`./scripts/simulator-udid.sh` prints one udid and exits 0; `SIMULATOR_NAME=NoSuchDevice ./scripts/simulator-udid.sh; test $? -eq 1`.</verify>
  <done>A single resolver; no device name hardcoded in the repo.</done>
</task>

<task type="auto">
  <name>Task 2: App skeleton on the tokens</name>
  <files>src/App/GrokBotLocatorApp.swift, src/App/RootView.swift</files>
  <action>GrokBotLocatorApp: `@main struct GrokBotLocatorApp: App`, one WindowGroup showing RootView — no entry point exists until this does.
RootView: a NavigationStack with a plain placeholder `Text`, NO font modifier — `dsFont` does not exist until 01-03 and a raw `.font(.system(size:))` would trip 01-05's guard. Plus `.navigationTitle` and `.padding(DSMetrics.screenMargin)`. 01-03 adds the token; 01-12 swaps in SettingsView.
SwiftUI only: no CoreLocation, no URLSession, no credential literals.</action>
  <verify>`xcodegen generate` first — 01-01 generated before src/App/ existed, so without it there is no @main and linking fails. Then `grep -nE 'font\(' src/App/RootView.swift; test $? -eq 1` (no font modifier yet); `UDID=$(./scripts/simulator-udid.sh) && xcodebuild build -scheme GrokBotLocator -destination "platform=iOS Simulator,id=$UDID" -derivedDataPath build/dd-02 | grep -q 'BUILD SUCCEEDED'`.
<human-check>Launch once: it opens portrait and rotating does not switch to landscape.</human-check></verify>
  <done>App links and builds; the placeholder is plain unstyled Text.</done>
</task>

<task type="auto">
  <name>Task 3: Seed the test bundle</name>
  <files>tests/SmokeTests.swift</files>
  <action>Swift Testing — `import Testing`, never XCTest — plus `@testable import GrokBotLocator`. One `@Test func appModuleIsLinked()` constructing RootView and asserting a trivial invariant with `#expect`, proving host and module wiring. No credentials, no network.</action>
  <verify>`xcodegen generate` first — the bundle predates tests/SmokeTests.swift. Then Task 2's command with `test` for `build`: output must contain `** TEST SUCCEEDED **`, name `appModuleIsLinked`, AND match `Test run with [1-9][0-9]* tests? in [0-9]+ suites? passed`. NOT `Executed N tests` — always 0 here. smoke.sh arrives in 01-04, so the pattern is spelled out.</verify>
  <done>Test bundle builds against the app host; one test passes.</done>
</task>
</tasks>
