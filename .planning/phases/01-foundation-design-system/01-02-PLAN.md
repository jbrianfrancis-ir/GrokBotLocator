---
phase: 01-foundation-design-system
plan: 02
wave: 2
depends_on: [01-01]
files_modified: [scripts/simulator-udid.sh, src/App/GrokBotLocatorApp.swift, src/App/RootView.swift, tests/SmokeTests.swift]
autonomous: true
requirements: [REQ-12]
must_haves:
  truths:
    - "simulator-udid.sh prints one udid for an available iOS 26 iPhone, non-zero if none"
    - "xcodebuild test builds the app and runs the bundle against its host with a non-zero test count"
    - "RootView renders at the 20pt DSTypography.body token, not the system .body style"
  artifacts: [scripts/simulator-udid.sh, src/App/RootView.swift, tests/SmokeTests.swift]
  key_links:
    - "GrokBotLocatorApp presents RootView, which reads the 01-01 tokens"
    - "SmokeTests imports the app module, proving test-host wiring"
---

<objective>
The first buildable slice: a shared simulator resolver, the `@main` entry point the target
needs before anything compiles, and a test proving host wiring.
</objective>

<context>
.planning/DESIGN.md and .planning/ARCHITECTURE.md are law. From 01-01: project.yml, DSTypography,
DSMetrics. Signing.xcconfig exists locally. An iOS 26 runtime with several iPhone models is
installed — never hardcode a device name. 01-03 wraps this into ./scripts/smoke.sh.
</context>

<tasks>
<task type="auto">
  <name>Task 1: Simulator resolver</name>
  <files>scripts/simulator-udid.sh</files>
  <action>Executable bash, `set -euo pipefail`. Prints exactly one udid on stdout, nothing else. Honour $SIMULATOR_NAME when set; else parse `xcrun simctl list devices available -j` with python3 and take the first available iPhone whose runtime id contains `iOS-26`. When nothing matches, say on stderr that no iOS 26 iPhone simulator was found and exit 1. Never hardcode a device name.</action>
  <verify>`./scripts/simulator-udid.sh` prints one udid and exits 0; `SIMULATOR_NAME=NoSuchDevice ./scripts/simulator-udid.sh; test $? -eq 1`.</verify>
  <done>A single resolver; no device name hardcoded in the repo.</done>
</task>

<task type="auto">
  <name>Task 2: App skeleton on the tokens</name>
  <files>src/App/GrokBotLocatorApp.swift, src/App/RootView.swift</files>
  <action>GrokBotLocatorApp: `@main struct GrokBotLocatorApp: App`, one WindowGroup showing RootView — no entry point exists until this does.
RootView: a NavigationStack with a placeholder Text at `.font(DSTypography.body)` — never `.font(.body)`, the system style rather than the token — plus `.navigationTitle` and `.padding(DSMetrics.screenMargin)`. 01-11 swaps the body for SettingsView.
SwiftUI only: no CoreLocation, no URLSession, no credential literals.</action>
  <verify>`xcodegen generate` first — 01-01 generated before src/App/ existed, so without it the target has no @main and linking fails. Then `grep -n 'font(\.body)' src/App/RootView.swift; test $? -eq 1`; `grep -q DSTypography.body src/App/RootView.swift`; `UDID=$(./scripts/simulator-udid.sh) && xcodebuild build -scheme GrokBotLocator -destination "platform=iOS Simulator,id=$UDID" -derivedDataPath build/dd-02 | grep -q 'BUILD SUCCEEDED'`.
<human-check>Launch once: it opens portrait and rotating does not switch to landscape.</human-check></verify>
  <done>App builds; its placeholder renders at the 20pt token.</done>
</task>

<task type="auto">
  <name>Task 3: Seed the test bundle</name>
  <files>tests/SmokeTests.swift</files>
  <action>Swift Testing — `import Testing`, never XCTest — plus `@testable import GrokBotLocator`. One `@Test func appModuleIsLinked()` constructing RootView and asserting a trivial invariant with `#expect`, proving host and module wiring. No fixture credentials, no network. Leave tests/.gitkeep.</action>
  <verify>`xcodegen generate` first — the bundle was generated before tests/SmokeTests.swift existed. Then Task 2's command with `test` for `build`: output must contain `** TEST SUCCEEDED **`, name `appModuleIsLinked`, AND report one executed test. Otherwise it reports success having run nothing.</verify>
  <done>Test bundle builds against the app host; one test passes.</done>
</task>
</tasks>
