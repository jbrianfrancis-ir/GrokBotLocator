---
phase: 01-foundation-design-system
plan: 06
wave: 6
depends_on: [01-05]
files_modified: [tests/KeychainCredentialStoreTests.swift, tests/CredentialLeakTests.swift]
autonomous: true
requirements: [REQ-01]
must_haves:
  truths:
    - "Round-trip, overwrite, empty-load-nil, and clear-then-nil are each proven by a passing test"
    - "A test proves that after a save, a full UserDefaults dump contains none of the three values"
    - "A test proves WebhookCredentials descriptions redact url, senderKey and headerName"
    - "No credential value appears in any tracked file: src/, tests/, project.yml, Signing.xcconfig.example"
  artifacts: [tests/KeychainCredentialStoreTests.swift, tests/CredentialLeakTests.swift]
  key_links:
    - "Tests construct KeychainCredentialStore with a throwaway service string, never the app's real one"
---

<objective>
Prove REQ-01: credentials survive a round-trip in the Keychain and appear nowhere else —
not in UserDefaults, not in a description, not in the repo.
</objective>

<context>
.planning/REQUIREMENTS.md REQ-01 acceptance is the target. From 01-05:
src/Core/Credentials/{WebhookCredentials,CredentialStore,KeychainCredentialStore}.swift.
From 01-02: ./scripts/smoke.sh verifies. Swift Testing, never XCTest.
Never use a real webhook url or sender key in a test — synthetic values only.
</context>

<tasks>
<task type="auto">
  <name>Task 1: Keychain round-trip tests</name>
  <files>tests/KeychainCredentialStoreTests.swift</files>
  <action>Swift Testing suite. Every test builds KeychainCredentialStore with a unique per-run service string ("test." + UUID().uuidString) so it never touches the app's real items, and calls clear() at the end. Values are obviously synthetic (e.g. an example.invalid host and a dummy key literal). Cases:
1. save then load returns identical url, senderKey, headerName.
2. a second save with different values overwrites — load returns only the new values, and no duplicate item exists.
3. load against a fresh unused service returns nil.
4. clear then load returns nil.
5. saving with headerName omitted/empty loads back WebhookCredentials.defaultHeaderName, while a missing url or senderKey still yields nil overall.</action>
  <verify>`./scripts/smoke.sh` exits 0 with all five tests passing.</verify>
  <done>Storage, overwrite, absence, and the header-name default are each covered by a passing test.</done>
</task>

<task type="auto">
  <name>Task 2: Leak tests and repo sweep</name>
  <files>tests/CredentialLeakTests.swift</files>
  <action>Swift Testing. Case 1: with a throwaway service, save synthetic credentials, then flatten `UserDefaults.standard.dictionaryRepresentation()` to strings and `#expect` that none of the three values occurs in it; clear afterwards. Case 2: build a WebhookCredentials from synthetic values and `#expect` that both `String(describing:)` and `String(reflecting:)` contain none of the three values, so an interpolation into a log cannot leak one.</action>
  <verify>`./scripts/smoke.sh` exits 0 with both tests passing; `grep -rniE 'Bearer [A-Za-z0-9]|sk-|https?://(?!example\.invalid)' src/ tests/ project.yml Signing.xcconfig.example` finds no real credential or endpoint (adjust to a basic grep if -P is unavailable).
<human-check>Install on device, enter the real url, key and header, force-quit and relaunch: all three are still populated. Then run `strings` on the built app binary and confirm the sender key and the webhook host do not appear.</human-check></verify>
  <done>Leak tests pass and no tracked file carries a credential value.</done>
</task>
</tasks>
