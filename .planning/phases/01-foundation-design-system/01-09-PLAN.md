---
phase: 01-foundation-design-system
plan: 09
wave: 9
depends_on: [01-08]
files_modified: [tests/KeychainCredentialStoreTests.swift, tests/CredentialLeakTests.swift]
autonomous: true
requirements: [REQ-01]
must_haves:
  truths:
    - "Round-trip, overwrite, empty-load-nil and clear-then-nil are each proven by a passing test"
    - "A test proves that after a save, a full UserDefaults dump contains none of the three values"
    - "A test proves WebhookCredentials descriptions redact url, senderKey and headerName"
    - "No credential value appears in any tracked file under src/, tests/, project.yml or Signing.xcconfig.example"
  artifacts: [tests/KeychainCredentialStoreTests.swift, tests/CredentialLeakTests.swift]
  key_links:
    - "Tests construct KeychainCredentialStore with a throwaway service string, never the app's real one"
---

<objective>
REQ-01's storage proven: Keychain round-trip behaviour and the absence of any credential leak.
</objective>

<context>
From 01-08: the keychain-access-groups entitlement — without it every SecItem call here
returns -34018. From 01-05: KeychainCredentialStore. From 01-03: ./scripts/smoke.sh.
</context>

<tasks>
<task type="auto">
  <name>Task 1: Keychain round-trip tests</name>
  <files>tests/KeychainCredentialStoreTests.swift</files>
  <action>A Swift Testing suite as `@Suite struct KeychainCredentialStoreTests { … }` — without a `@Suite` type no Suite line is emitted. Every test builds KeychainCredentialStore with a unique per-run service string ("test." + UUID().uuidString), so it never touches the app's real items, and calls clear() at the end. Values are obviously synthetic — an example.invalid host and a dummy key literal. Cases:
1. save then load returns identical url, senderKey and headerName.
2. a second save with different values overwrites: load returns only the new values and no duplicate item exists.
3. load against a fresh unused service returns nil.
4. clear then load returns nil.
5. saving with headerName empty loads back WebhookCredentials.defaultHeaderName, while a missing url or senderKey still makes load return nil overall.</action>
  <verify>`./scripts/smoke.sh` exits 0 AND `grep -qE 'Suite KeychainCredentialStoreTests passed'` on its log. The bare name is NOT enough — the compile line carries it whether or not a test ran.</verify>
  <done>Storage, overwrite, absence and the header-name default are each covered by a passing test.</done>
</task>

<task type="auto">
  <name>Task 2: Leak tests and repo sweep</name>
  <files>tests/CredentialLeakTests.swift</files>
  <action>Swift Testing, as `@Suite struct CredentialLeakTests { … }`.
Case 1: with a throwaway service, save synthetic credentials, flatten `UserDefaults.standard.dictionaryRepresentation()` to strings, and `#expect` that none of the three values occurs anywhere in it; clear afterwards.
Case 2: build a WebhookCredentials from synthetic values and `#expect` that both `String(describing:)` and `String(reflecting:)` contain none of the three, so interpolating one into a log line cannot leak it.</action>
  <verify>`./scripts/smoke.sh` exits 0 AND `grep -qE 'Suite CredentialLeakTests passed'` on its log (the bare name matches the compile line); `grep -rniE 'Bearer [A-Za-z0-9]{8}|sk-[A-Za-z0-9]|https://[a-z0-9.-]+\.[a-z]{2,}' src tests project.yml Signing.xcconfig.example GrokBotLocator.entitlements | grep -v '\.invalid'; test $? -eq 1` — `.invalid` is excluded because Task 1 mandates it as the synthetic fixture host; without the exclusion this verify forbids its own required fixture.
<human-check>Install on device, enter the real url, key and header, force-quit and relaunch: url and header name are still populated, and the sender key shows a "key saved" indicator with no value on screen and no reveal control (D-10). Then run `strings` on the built app binary and confirm neither the sender key nor the webhook host appears.</human-check></verify>
  <done>Leak tests pass and no tracked file carries a credential value.</done>
</task>
</tasks>
