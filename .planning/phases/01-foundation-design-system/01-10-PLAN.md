---
phase: 01-foundation-design-system
plan: 10
wave: 5
depends_on: [01-06, 01-07, 01-09]
files_modified: [src/Settings/SettingsView.swift, src/App/RootView.swift, src/App/GrokBotLocatorApp.swift]
autonomous: true
requirements: [REQ-01, REQ-12, SC-05, SC-06]
must_haves:
  truths:
    - "The app launches into a settings screen where url, key and header are the only inputs setup needs (SC-05 phase-1 half; ping is phase 2)"
    - "Entering all three, saving, force-quitting and relaunching leaves them set"
    - "A rejected save shows an on-screen body-size sentence saying what to fix, not a toast or status code"
    - "Setup is completable unaided at the default text size; at AX5 nothing clips or overlaps in either appearance"
    - "Outcome shows as symbol plus word plus colour; no glass behind text, fields or the save action"
  backstop_truths:
    - "Whether the sender key field repopulates after relaunch, or shows a saved-indicator with the value withheld, is unsettled by the requirements: both satisfy REQ-01"
  artifacts: [src/Settings/SettingsView.swift]
  key_links:
    - "GrokBotLocatorApp injects KeychainCredentialStore into SettingsModel; RootView presents SettingsView"
---

<objective>
The settings screen on the design system, wired to the Keychain store. No location, no
network, no ping.
</objective>

<context>
.planning/DESIGN.md is law. From 01-01/01-04/01-06/01-07: the tokens plus PingButton,
CredentialField and DSChrome. From 01-05: KeychainCredentialStore. From 01-09: SettingsModel.
From 01-03: ./scripts/smoke.sh. Navigation chrome gets glass automatically — add no glass code.
</context>

<tasks>
<task type="auto">
  <name>Task 1: SettingsView</name>
  <files>src/Settings/SettingsView.swift</files>
  <action>A VStack in a ScrollView, not a Form, so AX5 reflows freely: title at DSTypography.screenTitle, .screenMargin margins, .groupGap between groups, no fixed-height containers.
Three CredentialFields bound to SettingsModel: "Webhook URL" (URL keyboard, no autocorrect), "Sender key" (isSecure, reveal toggle), "Header name" (footnote naming the Authorization default).
A PingButton labelled "Save settings" in the bottom third for one-handed reach, plus a Clear action still meeting the 60pt floor.
Render `model.status` inline above the button at DSTypography.body: saved as symbol + word + DSPalette.success, error as symbol + sentence + .failure. No toast, no alert-only path.
Apply DSChrome to any custom surface so Reduce Transparency and Increase Contrast fall back to opaque fills. `.task { model.load() }`. VoiceOver labels throughout.</action>
  <verify>`./scripts/smoke.sh` exits 0; `grep -rniE 'glassEffect|Material|alert\(|Form\(' src/Settings/SettingsView.swift; test $? -eq 1`.</verify>
  <done>Three fields, save and clear, inline outcome text, from design-system components.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the app and close the phase</name>
  <files>src/App/GrokBotLocatorApp.swift, src/App/RootView.swift</files>
  <action>GrokBotLocatorApp constructs one KeychainCredentialStore, builds a SettingsModel from it and passes that to RootView. RootView replaces its 01-02 placeholder with SettingsView inside the NavigationStack, keeping the navigation title so the screen carries system chrome. Nothing else: no second screen, no CoreLocation, no URLSession.</action>
  <verify>`./scripts/smoke.sh` exits 0.
<human-check>On device at default text size, in sunlight if possible: enter url, key and header, save, force-quit, relaunch — all three still set, and those were the only inputs (SC-05, SC-06). An http url gives an on-screen error naming the field and fix.</human-check>
<human-check>At AX5 in light and dark nothing clips or overlaps. An Accessibility Inspector audit reports zero contrast and zero hit-target failures (REQ-12).</human-check>
<human-check>Backstop: on relaunch should the sender key field repopulate, or show a "key saved" indicator with the value withheld?</human-check></verify>
  <done>App opens on the settings screen; smoke passes.</done>
</task>
</tasks>
