---
phase: 01-foundation-design-system
plan: 08
wave: 8
depends_on: [01-07]
files_modified: [project.yml, GrokBotLocator.entitlements]
autonomous: true
requirements: [REQ-01]
must_haves:
  truths:
    - "The app target carries a keychain-access-groups entitlement, so SecItem calls in 01-09 succeed instead of returning -34018 errSecMissingEntitlement"
    - "The entitlement uses build-setting substitution, so no literal bundle id or team id enters the repo"
    - "The test bundle runs hosted by the app and inherits that entitlement"
  artifacts: [GrokBotLocator.entitlements]
  key_links:
    - "project.yml CODE_SIGN_ENTITLEMENTS points at GrokBotLocator.entitlements for the app target"
---

<objective>
The entitlement wiring the Keychain needs. Without it every SecItem call in 01-09 fails,
so this must land before any test touches the Keychain.
</objective>

<context>
Verified on this machine: an unsigned, unhosted test bundle returns -34018 on every SecItem
call; adding a keychain-access-groups entitlement with ad-hoc signing and a hosted test
target makes the round trip pass. ARCHITECTURE.md forbids a literal bundle id or team id in
any tracked file, so the entitlement must use build-setting substitution.
</context>

<tasks>
<task type="auto">
  <name>Task 1: Entitlement and project wiring</name>
  <files>project.yml, GrokBotLocator.entitlements</files>
  <action>GrokBotLocator.entitlements: a plist whose `keychain-access-groups` array holds exactly `$(AppIdentifierPrefix)$(PRODUCT_BUNDLE_IDENTIFIER)`. Use the substitution, never a literal — both values resolve from the gitignored Signing.xcconfig at build time.
project.yml: add `CODE_SIGN_ENTITLEMENTS: GrokBotLocator.entitlements` to the app target's settings. The app target already sets CODE_SIGN_IDENTITY "-" from 01-01; do not add DEVELOPMENT_TEAM.
Do NOT add TEST_HOST or BUNDLE_LOADER by hand: XcodeGen emits both from the test target's `dependencies: [target: GrokBotLocator]`, already present since 01-01.
Never pass CODE_SIGNING_ALLOWED=NO anywhere — it strips entitlements and reinstates the -34018 failure.</action>
  <verify>`grep -q 'keychain-access-groups' GrokBotLocator.entitlements`; `grep -rnE 'com\.[a-z0-9]+\.[a-z0-9]+|[A-Z0-9]{10}' GrokBotLocator.entitlements; test $? -eq 1` (no literal bundle or team id); `grep -q CODE_SIGN_ENTITLEMENTS project.yml`; `xcodegen generate` exits 0; then `grep -c 'TEST_HOST' GrokBotLocator.xcodeproj/project.pbxproj` reports at least 1, proving hosted tests; `./scripts/smoke.sh` exits 0.</verify>
  <done>The app carries the entitlement, tests are hosted, and no identifier literal is committed.</done>
</task>
</tasks>
