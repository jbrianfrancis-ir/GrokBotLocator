---
phase: 01-foundation-design-system
plan: 03
wave: 3
depends_on: [01-02]
files_modified: [scripts/smoke.sh]
autonomous: true
requirements: [REQ-12]
must_haves:
  truths:
    - "./scripts/smoke.sh exits 0 with ** TEST SUCCEEDED ** and a non-zero executed-test count"
    - "With Signing.xcconfig renamed away, smoke.sh exits non-zero and names that file"
    - "With no available iOS 26 iPhone simulator, smoke.sh exits non-zero saying so"
    - "smoke.sh fails, naming file and line, when a font-size literal below 17 appears under src/ outside DSTypography"
    - "smoke.sh never passes CODE_SIGNING_ALLOWED=NO, so entitlements survive for Keychain tests"
  artifacts: [scripts/smoke.sh]
  key_links:
    - "smoke.sh calls scripts/simulator-udid.sh for its destination, never naming a device"
    - "smoke.sh runs xcodegen generate before xcodebuild test, so files later plans add are built"
---

<objective>
The ARCHITECTURE-mandated gate every later plan is verified against, carrying the repo-wide
type-scale guard that keeps REQ-12 enforced as new views land.
</objective>

<context>
.planning/ARCHITECTURE.md Smoke section is law: smoke.sh runs xcodegen generate, builds for an
iOS 26 simulator and runs the test bundle; pass is exit 0 plus ** TEST SUCCEEDED **.
From 01-02: simulator-udid.sh, the skeleton, tests/SmokeTests.swift. From 01-01: project.yml
and DSTypography, the only file allowed a font-size literal.
</context>

<tasks>
<task type="auto">
  <name>Task 1: The smoke gate</name>
  <files>scripts/smoke.sh</files>
  <action>Executable bash (chmod +x), `set -euo pipefail`, cd to the repo root from $0, echoing each step:
1. If Signing.xcconfig is absent, print `Signing.xcconfig is missing - copy Signing.xcconfig.example and set DEVELOPMENT_TEAM and PRODUCT_BUNDLE_IDENTIFIER`, exit 1.
2. `xcodegen generate`.
3. `UDID=$(./scripts/simulator-udid.sh)`, propagating a non-zero exit and its message rather than falling back to a device.
4. `xcodebuild test -scheme GrokBotLocator -destination "platform=iOS Simulator,id=$UDID" -derivedDataPath "${SMOKE_DERIVED_DATA:-build/dd-smoke}"`, tee'd to a log beside that path. $SMOKE_DERIVED_DATA overrides it; the shared default reuses a warm cache.
5. Exit 0 only if the log has `** TEST SUCCEEDED **` AND matches `Test run with [1-9][0-9]* test` (Swift Testing's count) — NOT `Executed N tests`, the XCTest line, always 0 here. Else print the tail and exit 1. A scheme missing its test target runs nothing yet succeeds; the count closes it.
Never pass CODE_SIGNING_ALLOWED=NO: it strips entitlements and every Keychain call fails -34018.
Print no credential values; never echo Signing.xcconfig. Task 2 adds the token guard.</action>
  <verify>`./scripts/smoke.sh` exits 0, prints ** TEST SUCCEEDED ** and a non-zero test count.
`mv Signing.xcconfig /tmp/ && ./scripts/smoke.sh; test $? -eq 1`, message names the file, restore it.
`SIMULATOR_NAME=NoSuchDevice ./scripts/smoke.sh; test $? -eq 1`.</verify>
  <done>Gate passes clean; fails loudly on missing xcconfig, runtime, or zero tests.</done>
</task>

<task type="auto">
  <name>Task 2: Repo-wide type-scale guard</name>
  <files>scripts/smoke.sh</files>
  <action>Insert a guard step into smoke.sh before `xcodegen generate` that fails the run when DESIGN.md's type floor is broken: search every .swift under src/ except Core/DesignSystem/DSTypography.swift for a font-size literal below 17 or a raw `.font(.system(size:`, print each offending file and line, and exit 1 if any are found.
DSTypography is the sole exemption, being the one file allowed a size literal. This keeps REQ-12's "nothing below 17pt" enforced in the components and screens later plans add.</action>
  <verify>`./scripts/smoke.sh` exits 0 clean. Add a temporary src/ file with `.font(.system(size: 12))`, run `./scripts/smoke.sh; test $? -eq 1`, confirm the output names that file and line, delete it. Repeat with the literal inside DSTypography.swift: smoke still exits 0.</verify>
  <done>Sub-17pt type anywhere under src/ fails the gate; DSTypography is the only exemption.</done>
</task>
</tasks>
