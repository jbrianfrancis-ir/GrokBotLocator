---
phase: 01-foundation-design-system
plan: 07
wave: 4
depends_on: [01-03, 01-04]
files_modified: [src/Core/DesignSystem/PingOutcomeRow.swift, src/Core/DesignSystem/DSChrome.swift]
autonomous: true
requirements: [REQ-12]
must_haves:
  truths:
    - "PingOutcomeRow shows each outcome as symbol plus word plus colour, never colour alone"
    - "The row reflows without clipping at the default text size and at AX5, both modes"
    - "DSChrome resolves to an opaque fill when Reduce Transparency is on or contrast is increased"
    - "Glass arrives only via system chrome; no source file calls glassEffect"
  artifacts: [src/Core/DesignSystem/PingOutcomeRow.swift, src/Core/DesignSystem/DSChrome.swift]
  key_links:
    - "DSChrome reads accessibilityReduceTransparency and colorSchemeContrast from the environment"
    - "PingOutcomeRow takes symbol, word and colour from one PingOutcome case"
---

<objective>
The outcome row, plus DESIGN.md's chrome rule made concrete: system chrome carries glass and
custom surfaces fall back to opaque fills under the accessibility settings.
</objective>

<context>
.planning/DESIGN.md is law — the Chrome group and the Liquid Glass rule. RESEARCH.md Q3:
toolbars, tab bars and sheets adopt glass automatically with no code, you opt in explicitly
only for custom controls, and `accessibilityReduceTransparency` / `colorSchemeContrast` are
confirmed environment values. Do NOT code against `.buttonStyle(.glass)` or `glassEffect`:
RESEARCH.md lists the former unverified, and DESIGN.md bars glass from the primary action,
body text and fields anyway. Tokens from 01-01/01-04. From 01-03:
`SMOKE_DERIVED_DATA=build/dd-07 ./scripts/smoke.sh`.
</context>

<tasks>
<task type="auto">
  <name>Task 1: PingOutcomeRow</name>
  <files>src/Core/DesignSystem/PingOutcomeRow.swift</files>
  <action>Presentational only — stores nothing, fetches nothing, triggers nothing; history is a later phase. A value type holding timestamp, latitude, longitude, label and a `PingOutcome` enum (sent, queued, failed) where each case supplies SF Symbol name, word and DSPalette colour together, so a colour can never appear without its word.
Timestamp and coordinates at DSTypography.secondary, label at .body, symbol + word + colour. Flexible frames so AX5 reflows vertically: no fixed-height container, no `.lineLimit(1)`. `accessibilityElement(children: .combine)`, labelled with the outcome word.
`#Preview`s of all three outcomes, light and dark, at the default size and at `.accessibility5`.</action>
  <verify>`SMOKE_DERIVED_DATA=build/dd-07 ./scripts/smoke.sh` exits 0; `grep -n lineLimit src/Core/DesignSystem/PingOutcomeRow.swift; test $? -eq 1`.
<human-check>At AX5 in light and dark each outcome shows symbol, word and colour, no clipping.</human-check></verify>
  <done>A pure view of all three outcomes, previewed at both ends of the range.</done>
</task>

<task type="auto">
  <name>Task 2: DSChrome and the transparency fallback</name>
  <files>src/Core/DesignSystem/DSChrome.swift</files>
  <action>A ViewModifier plus a `View` extension giving custom surfaces DESIGN.md's fallback: read `\.accessibilityReduceTransparency` and `\.colorSchemeContrast` from the environment and, when either asks, resolve the background to a fully opaque DSPalette fill; otherwise a standard non-glass background. Never call `glassEffect` or a glass button style.
Doc-comment the deferral: system nav bars and toolbars get glass automatically, so chrome needs no code here; explicit custom glass waits for a later phase.
`#Preview`s of the modifier with reduce-transparency on and off.</action>
  <verify>`SMOKE_DERIVED_DATA=build/dd-07 ./scripts/smoke.sh` exits 0; `grep -rniE 'glassEffect|buttonStyle\(\.glass' src/; test $? -eq 1`; `grep -cE 'accessibilityReduceTransparency|colorSchemeContrast' src/Core/DesignSystem/DSChrome.swift` reports >= 2.
<human-check>With Reduce Transparency on, then Increase Contrast on, the surface is fully opaque.</human-check></verify>
  <done>Custom surfaces honour both settings; no explicit glass call anywhere.</done>
</task>
</tasks>
