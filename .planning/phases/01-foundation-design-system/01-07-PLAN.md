---
phase: 01-foundation-design-system
plan: 07
wave: 7
depends_on: [01-06]
files_modified: [src/Core/DesignSystem/PingButton.swift, src/Core/DesignSystem/CredentialField.swift]
autonomous: true
requirements: [REQ-12, SC-06]
must_haves:
  truths:
    - "PingButton is full-width, >=88pt tall, labelled at 28pt semibold, on an opaque fill"
    - "PingButton's pressed state changes its fill; in flight a spinner sits beside a changed word"
    - "CredentialField has a visible label above the input, never placeholder-only; its secure variant has NO reveal control and can show a saved-indicator instead of a value (D-10)"
    - "Both render without clipping or overlap at the default text size and at AX5, light and dark"
    - "Every control carries a VoiceOver label; neither uses a glass or material background"
  artifacts: [src/Core/DesignSystem/PingButton.swift, src/Core/DesignSystem/CredentialField.swift]
  key_links:
    - "Both read every size and colour from the phase tokens, never literals"
---

<objective>
The two components the settings screen is built from — primary action and credential input —
as presentational SwiftUI on the phase tokens.
</objective>

<context>
.planning/DESIGN.md is law: read Components and Rules fully. From 01-01/01-05: DSTypography,
DSMetrics, DSPalette in src/Core/DesignSystem/. From 01-04: ./scripts/smoke.sh. No glass, no materials: DESIGN.md forbids glass behind the
primary action and credential fields, which is everything this plan renders.
</context>

<tasks>
<task type="auto">
  <name>Task 1: PingButton</name>
  <files>src/Core/DesignSystem/PingButton.swift</files>
  <action>A button taking a title, an `isInFlight` Bool, and an action. Full width, minHeight DSMetrics.primaryActionHeight, radius DSMetrics.primaryActionRadius, label via `.dsFont(.actionLabel)`, opaque DSPalette.primaryAction fill.
In flight: a ProgressView beside a changed word, never a spinner alone, button disabled.
A custom ButtonStyle gives the pressed state a different fill, not merely reduced opacity.
Set accessibilityLabel plus an accessibilityValue speaking the in-flight state.
`#Preview`s: idle and in-flight, light and dark, at the system default size (SC-06) and at `.accessibility5` (REQ-12) — both ends, here and in Task 2.</action>
  <verify>`./scripts/smoke.sh` exits 0; `grep -rniE 'glass|Material' src/Core/DesignSystem/PingButton.swift; test $? -eq 1`.
<human-check>Press and hold in the preview: the fill visibly changes. Toggle in-flight: a word changes beside the spinner. VoiceOver reads label and in-flight state.</human-check></verify>
  <done>Compiles from tokens only, previewed at default size and AX5 in both appearances.</done>
</task>

<task type="auto">
  <name>Task 2: CredentialField</name>
  <files>src/Core/DesignSystem/CredentialField.swift</files>
  <action>An input bound to a String, with a visible Text label above the field via `.dsFont(.body)`, never placeholder-only. The `isSecure` variant renders a SecureField with NO reveal control — a stored credential is never displayed (D-10). It takes an optional `savedIndicator: String?`; when set and the field is untouched it shows that text (e.g. "key saved") in place of any value, leaving the binding empty.
Field minHeight DSMetrics.minTapTarget, opaque background, radius .cornerRadius. Autocorrect and autocapitalisation off. Never print, log, or default the bound value. An optional footnote uses `.dsFont(.secondary)`.
`#Preview`s: plain, secure, and secure-with-indicator; light and dark; default size and AX5.</action>
  <verify>`./scripts/smoke.sh` exits 0; `grep -rniE 'print\(|NSLog|glass|Material' src/Core/DesignSystem/CredentialField.swift; test $? -eq 1`.
<human-check>At AX5 in both appearances label, field and indicator reflow without clipping; VoiceOver reads the label and the saved-indicator. No control anywhere reveals a secure value.</human-check></verify>
  <done>Visible label, no reveal control, saved-indicator mode, previews at both sizes.</done>
</task>
</tasks>
