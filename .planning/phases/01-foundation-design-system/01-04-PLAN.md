---
phase: 01-foundation-design-system
plan: 04
wave: 4
depends_on: [01-03]
files_modified: [src/Core/DesignSystem/PingButton.swift, src/Core/DesignSystem/CredentialField.swift, src/Core/DesignSystem/PingOutcomeRow.swift]
autonomous: true
requirements: [REQ-12]
must_haves:
  truths:
    - "PingButton is full-width, >=88pt tall, 28pt semibold, with distinct pressed and in-flight states"
    - "CredentialField has a visible label above the input, never placeholder-only, and a reveal toggle when secure"
    - "PingOutcomeRow shows each outcome as symbol plus word plus colour, never colour alone"
    - "At AX5 in light and dark all three reflow with no clipping or overlap, every control has a VoiceOver label, and none uses glass"
  artifacts: [src/Core/DesignSystem/PingButton.swift, src/Core/DesignSystem/CredentialField.swift, src/Core/DesignSystem/PingOutcomeRow.swift]
  key_links:
    - "All three read sizes and colours from the 01-03 tokens, never literals"
---

<objective>
The three DESIGN.md components, as presentational SwiftUI on the 01-03 tokens.
</objective>

<context>
.planning/DESIGN.md is law — read Components and Rules fully. From 01-03: DSTypography,
DSMetrics, DSPalette in src/Core/DesignSystem/. From 01-02: ./scripts/smoke.sh verifies.
Standard SwiftUI, no Liquid Glass.
</context>

<tasks>
<task type="auto">
  <name>Task 1: PingButton</name>
  <files>src/Core/DesignSystem/PingButton.swift</files>
  <action>Button taking a title, an `isInFlight` Bool, and an action. Full width, minHeight DSMetrics.primaryActionHeight, radius .primaryActionRadius, DSTypography.actionLabel, opaque DSPalette.primaryAction fill. In flight: ProgressView beside a changed word (never a spinner alone), disabled. A ButtonStyle gives a distinct pressed state by changing the fill, not opacity. accessibilityLabel plus accessibilityValue speaking the in-flight state. `#Preview`s idle and in-flight, light and dark, each with `.environment(\.dynamicTypeSize, .accessibility5)` — same in tasks 2 and 3.</action>
  <verify>`./scripts/smoke.sh` exits 0; `grep -niE 'glass|Material' PingButton.swift` is empty.</verify>
  <done>Compiles, tokens only, previews at AX5 both appearances.</done>
</task>

<task type="auto">
  <name>Task 2: CredentialField</name>
  <files>src/Core/DesignSystem/CredentialField.swift</files>
  <action>Input bound to a String. Visible Text label above the field at DSTypography.body — never placeholder-only. `isSecure` renders SecureField with an eye / eye.slash reveal toggle, >= DSMetrics.minTapTarget square, with a passed-in accessibilityLabel. Field minHeight .minTapTarget, opaque background, radius .cornerRadius. Autocorrection and autocapitalisation off. Never print, log, or default the bound value. Optional footnote at DSTypography.secondary. `#Preview`s plain and secure.</action>
  <verify>`./scripts/smoke.sh` exits 0; `grep -niE 'print\(|NSLog|os_log|glass' CredentialField.swift` is empty.</verify>
  <done>Visible label, 60pt reveal toggle, no logging.</done>
</task>

<task type="auto">
  <name>Task 3: PingOutcomeRow</name>
  <files>src/Core/DesignSystem/PingOutcomeRow.swift</files>
  <action>Presentational only — stores nothing, fetches nothing, triggers nothing (history is a later phase). A value type holding timestamp, latitude, longitude, label, and a `PingOutcome` enum (sent, queued, failed), each case supplying an SF Symbol name, a word, and a DSPalette colour. Timestamp and coordinates at DSTypography.secondary, label at .body, outcome as symbol + word + colour. VStack with flexible frames so AX5 reflows vertically — no fixed-height text container, no `.lineLimit(1)`. accessibilityElement(children: .combine) with a label speaking the outcome word. `#Preview` all three outcomes.</action>
  <verify>`./scripts/smoke.sh` exits 0; `grep -n 'lineLimit' PingOutcomeRow.swift` is empty.
<human-check>All three previews at AX5, light and dark: nothing clips or overlaps; each outcome shows symbol, word, colour.</human-check></verify>
  <done>Pure view covering all three outcomes.</done>
</task>
</tasks>
