---
phase: 01-foundation-design-system
plan: 12
wave: 12
depends_on: [01-11]
files_modified: [src/Settings/SettingsView.swift, src/App/RootView.swift, src/App/GrokBotLocatorApp.swift]
autonomous: true
requirements: [REQ-01, REQ-12, SC-05, SC-06]
must_haves:
  truths:
    - "The app launches into a settings screen where url, key and header are the only inputs setup needs (SC-05 phase-1 half; ping is phase 2)"
    - "A rejected save shows an on-screen body-size sentence saying what to fix, not a toast or status code"
    - "Setup is completable unaided at the default text size; at AX5 nothing clips or overlaps in either appearance"
    - "Outcome shows as symbol plus word plus colour; no glass behind text, fields or the save action"
    - "After saving all three and force-quitting, relaunch repopulates url and header; the key field is empty with a saved indicator, and no control anywhere reveals it (D-10)"
  artifacts: [src/Settings/SettingsView.swift]
  key_links:
    - "GrokBotLocatorApp injects KeychainCredentialStore into SettingsModel; RootView presents SettingsView"
---

<objective>
The settings screen on the design system, wired to the Keychain store. No location, no
network, no ping.
</objective>

<context>
.planning/DESIGN.md is law. From 01-01/01-05/01-07/01-08: the tokens plus PingButton,
CredentialField and DSChrome. From 01-06: KeychainCredentialStore. From 01-11: SettingsModel.
From 01-04: ./scripts/smoke.sh. Navigation chrome gets glass automatically — add no glass code.
</context>

<tasks>
<task type="auto">
  <name>Task 1: SettingsView</name>
  <files>src/Settings/SettingsView.swift</files>
  <action>A VStack in a ScrollView, not a Form, so AX5 reflows: title via `.dsFont(.screenTitle)`, .screenMargin margins, .groupGap between groups, no fixed-height containers.
Three CredentialFields bound to SettingsModel: "Webhook URL" (URL keyboard, no autocorrect), "Sender key" (isSecure, NO reveal toggle): when hasStoredKey and untouched, render a "key saved" indicator and leave the field empty — the key is never displayed, revealed, or pre-filled (D-10); typing replaces it, "Header name" (footnote naming the Authorization default).
A PingButton labelled "Save settings" in the bottom third for one-handed reach, plus a Clear action still meeting the 60pt floor.
Render `model.status` inline above the button via `.dsFont(.body)`: saved as symbol + word + DSPalette.success, error as symbol + sentence + .failure. No toast, no alert-only path.
Apply DSChrome to any custom surface so Reduce Transparency and Increase Contrast fall back to opaque fills. `.task { model.load() }`. VoiceOver labels throughout.</action>
  <verify>`./scripts/smoke.sh` exits 0; `grep -rniE 'glassEffect|Material|alert\(|Form *\{' src/Settings/SettingsView.swift; test $? -eq 1`.</verify>
  <done>Three fields, save and clear, inline outcome text, from design-system components.</done>
</task>

<task type="auto">
  <name>Task 2: Wire app, close phase</name>
  <files>src/App/GrokBotLocatorApp.swift, src/App/RootView.swift</files>
  <action>GrokBotLocatorApp constructs one KeychainCredentialStore, builds a SettingsModel from it, passes that to RootView. RootView replaces its 01-03 placeholder with SettingsView in the NavigationStack, keeping the nav title so the screen carries system chrome. Nothing else: no second screen, no CoreLocation, no URLSession.</action>
  <verify>`./scripts/smoke.sh` exits 0.
<human-check>On device at default text size, in sunlight if possible: enter url, key and header, save, force-quit, relaunch — url and header repopulate, the sender key shows a "key saved" indicator with no value and no reveal control anywhere (D-10), and those three were the only inputs (SC-05, SC-06). An http url gives an on-screen error naming the field and fix.</human-check>
<human-check>At AX5 in light and dark nothing clips or overlaps. An Accessibility Inspector audit reports zero contrast and zero hit-target failures (REQ-12).</human-check></verify>
  <done>App opens on the settings screen; smoke passes.</done>
</task>
</tasks>
