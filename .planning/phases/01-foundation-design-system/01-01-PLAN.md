---
phase: 01-foundation-design-system
plan: 01
wave: 1
depends_on: []
files_modified: [project.yml, Signing.xcconfig.example, src/.gitkeep, tests/.gitkeep, src/Core/DesignSystem/DSTypography.swift, src/Core/DesignSystem/DSMetrics.swift]
autonomous: true
requirements: [REQ-12]
user_setup:
  - "Create gitignored Signing.xcconfig from the .example with your DEVELOPMENT_TEAM and PRODUCT_BUNDLE_IDENTIFIER. Never commit it."
must_haves:
  truths:
    - "xcodegen generate produces the xcodeproj with a GrokBotLocator scheme and both targets"
    - "The generated scheme runs GrokBotLocatorTests, so xcodebuild test never passes over zero tests"
    - "DSTypography carries body 20, secondary 17, action 28 semibold, title 34 bold; all scaled, none below 17"
    - "DSMetrics carries 8/24/16pt spacing, 60pt tap target, 88pt action, 16/28pt radii"
    - "No bundle id or team id in any tracked file"
  artifacts: [project.yml, src/Core/DesignSystem/DSTypography.swift]
  key_links:
    - "project.yml globs src/ into the app target, tests/ into GrokBotLocatorTests"
    - "project.yml configFiles point at Signing.xcconfig"
---

<objective>
The XcodeGen spec and the DESIGN.md tokens every later view builds on.
</objective>

<context>
.planning/ARCHITECTURE.md and .planning/DESIGN.md are law — read DESIGN.md Tokens fully.
Greenfield: src/ and tests/ do not exist. Xcode 26.6, Swift 6.3.3, xcodegen 2.46
</context>

<tasks>
<task type="auto">
  <name>Task 1: project.yml, scheme, signing</name>
  <files>project.yml, Signing.xcconfig.example, src/.gitkeep, tests/.gitkeep</files>
  <action>XcodeGen 2.46.0. name GrokBotLocator; options.deploymentTarget.iOS "26.0". No `packages:` key — zero dependencies is law.
App target: application, iOS, sources [src]; SWIFT_VERSION 6.0, TARGETED_DEVICE_FAMILY "1", GENERATE_INFOPLIST_FILE YES, INFOPLIST_KEY_UISupportedInterfaceOrientations "UIInterfaceOrientationPortrait", CODE_SIGN_IDENTITY "-"; configFiles Debug+Release Signing.xcconfig.
Neither PRODUCT_BUNDLE_IDENTIFIER nor DEVELOPMENT_TEAM may appear in project.yml.
Test target GrokBotLocatorTests: bundle.unit-test, sources [tests], dependencies [target: GrokBotLocator], SWIFT_VERSION 6.0. 01-08 adds TEST_HOST wiring.
Scheme GrokBotLocator — nested mapping; XcodeGen has no dotted-key form and dies at parse:
```yaml
schemes:
  GrokBotLocator:
    build: {targets: {GrokBotLocator: all, GrokBotLocatorTests: [test]}}
    test: {targets: [GrokBotLocatorTests]}
```
Both halves required: miss either and `xcodebuild test` runs zero tests yet reports success.
Add src/.gitkeep AND tests/.gitkeep — XcodeGen validates source dirs at parse time and fails without them; nothing removes them. Signing.xcconfig.example: commented placeholders only.</action>
  <verify>`xcodegen generate` exits 0; `grep -nE 'DEVELOPMENT_TEAM|PRODUCT_BUNDLE_IDENTIFIER' project.yml; test $? -eq 1`; then `grep -q 'GrokBotLocatorTests.xctest' GrokBotLocator.xcodeproj/xcshareddata/xcschemes/GrokBotLocator.xcscheme` — assert the GENERATED scheme, not the spec text, which greps clean on invalid syntax too.</verify>
  <done>Project generates with both targets and a test-running scheme.</done>
</task>

<task type="auto">
  <name>Task 2: Type and metric tokens</name>
  <files>src/Core/DesignSystem/DSTypography.swift, src/Core/DesignSystem/DSMetrics.swift</files>
  <action>DSTypography: static `Font`s from `.system(size:weight:)` with `relativeTo:` (or `@ScaledMetric`) so each scales to AX5 — `body` 20 regular, `secondary` 17 regular, `actionLabel` 28 semibold, `screenTitle` 34 bold. Only file where a font-size literal may appear; none below 17.
DSMetrics: static CGFloats — spacingBase 8, groupGap 24, screenMargin 16, minTapTarget 60, primaryActionHeight 88, cornerRadius 16, primaryActionRadius 28. Namespaces only, no view code.</action>
  <verify>`grep -nE 'size: *([0-9]|1[0-6])\b' src/Core/DesignSystem/DSTypography.swift; test $? -eq 1`; `grep -c static src/Core/DesignSystem/DSMetrics.swift` >= 7.</verify>
  <done>Both token files carry every DESIGN.md Tokens value.</done>
</task>
</tasks>
