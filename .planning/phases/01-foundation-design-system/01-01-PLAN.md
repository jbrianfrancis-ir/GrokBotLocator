---
phase: 01-foundation-design-system
plan: 01
wave: 1
depends_on: []
files_modified: [project.yml, Signing.xcconfig.example, tests/.gitkeep, src/Core/DesignSystem/DSTypography.swift, src/Core/DesignSystem/DSMetrics.swift]
autonomous: true
requirements: [REQ-12]
user_setup:
  - "Create gitignored Signing.xcconfig from Signing.xcconfig.example with your real DEVELOPMENT_TEAM and PRODUCT_BUNDLE_IDENTIFIER. Never commit it."
must_haves:
  truths:
    - "xcodegen generate produces the xcodeproj with a GrokBotLocator scheme and both targets"
    - "The scheme lists GrokBotLocatorTests in both build.targets and test.targets, so xcodebuild test runs the bundle, never passing over zero tests"
    - "DSTypography carries body 20, secondary 17, action 28 semibold, title 34 bold, all scaled, none below 17"
    - "DSMetrics carries 8/24/16pt spacing, 60pt tap target, 88pt action height, 16/28pt radii"
    - "No bundle id or team id in any tracked file"
  artifacts: [project.yml, src/Core/DesignSystem/DSTypography.swift]
  key_links:
    - "project.yml globs src/ into the app target, tests/ into GrokBotLocatorTests"
    - "project.yml configFiles point at Signing.xcconfig"
---

<objective>
The XcodeGen spec and the DESIGN.md tokens every later view builds on. 01-02 adds the first
build; 01-08 adds the Keychain entitlement wiring.
</objective>

<context>
.planning/ARCHITECTURE.md and .planning/DESIGN.md are law — read DESIGN.md Tokens fully.
Greenfield: src/ and tests/ do not exist. Xcode 26.6, Swift 6.3.3, xcodegen 2.46.0.
</context>

<tasks>
<task type="auto">
  <name>Task 1: project.yml, scheme and signing defaults</name>
  <files>project.yml, Signing.xcconfig.example, tests/.gitkeep</files>
  <action>XcodeGen 2.46.0. name GrokBotLocator; options.deploymentTarget.iOS "26.0". Never add a `packages:` key — zero dependencies is law.
App target: application, iOS, sources [src]; settings SWIFT_VERSION 6.0, TARGETED_DEVICE_FAMILY "1", GENERATE_INFOPLIST_FILE YES, INFOPLIST_KEY_UISupportedInterfaceOrientations "UIInterfaceOrientationPortrait", CODE_SIGN_IDENTITY "-" (ad-hoc simulator signing); configFiles Debug and Release both Signing.xcconfig.
Neither PRODUCT_BUNDLE_IDENTIFIER nor DEVELOPMENT_TEAM may appear in project.yml.
Test target GrokBotLocatorTests: bundle.unit-test, sources [tests], dependencies [target: GrokBotLocator], SWIFT_VERSION 6.0. 01-08 adds its TEST_HOST wiring.
Scheme GrokBotLocator with `build.targets: {GrokBotLocator: all, GrokBotLocatorTests: [test]}` and `test.targets: [GrokBotLocatorTests]`. Both are required: miss the test target in either and `xcodebuild test` runs zero tests yet still reports success.
Add tests/.gitkeep so the glob resolves before any test file exists; nothing removes it. Signing.xcconfig.example: commented placeholders only.</action>
  <verify>`xcodegen generate` exits 0; `grep -nE 'DEVELOPMENT_TEAM|PRODUCT_BUNDLE_IDENTIFIER' project.yml; test $? -eq 1`; `grep -n test.targets project.yml; test $? -eq 0`.</verify>
  <done>Project generates with both targets and a scheme that runs tests.</done>
</task>

<task type="auto">
  <name>Task 2: Type and metric tokens</name>
  <files>src/Core/DesignSystem/DSTypography.swift, src/Core/DesignSystem/DSMetrics.swift</files>
  <action>DSTypography: static `Font` values from `.system(size:weight:)` with a `relativeTo:` text style (or `@ScaledMetric`) so each scales to AX5 — `body` 20 regular, `secondary` 17 regular, `actionLabel` 28 semibold, `screenTitle` 34 bold. This is the only file where a font-size literal may appear, and none below 17 may appear in it.
DSMetrics: static CGFloats — spacingBase 8, groupGap 24, screenMargin 16, minTapTarget 60, primaryActionHeight 88, cornerRadius 16, primaryActionRadius 28. Plain namespaces, no view code; 01-02 compiles them.</action>
  <verify>`grep -nE 'size: *([0-9]|1[0-6])\b' src/Core/DesignSystem/DSTypography.swift; test $? -eq 1`; `grep -c static src/Core/DesignSystem/DSMetrics.swift` >= 7.</verify>
  <done>Both token files carry every DESIGN.md Tokens value, inside the src/ glob.</done>
</task>
</tasks>
