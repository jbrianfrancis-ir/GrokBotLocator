---
phase: 01-foundation-design-system
plan: 01
wave: 1
depends_on: []
files_modified: [project.yml, Signing.xcconfig.example, src/App/GrokBotLocatorApp.swift, src/App/RootView.swift]
autonomous: true
requirements: [REQ-01, REQ-12]
user_setup:
  - "Create gitignored Signing.xcconfig at the repo root from Signing.xcconfig.example, filling in your real DEVELOPMENT_TEAM and PRODUCT_BUNDLE_IDENTIFIER. Builds fail naming this file if it is absent. Never commit it."
must_haves:
  truths:
    - "xcodegen generate produces GrokBotLocator.xcodeproj with a GrokBotLocator scheme"
    - "The app builds and launches portrait on an iOS 26 simulator"
    - "No team id, bundle id, or credential value appears in any tracked file"
  artifacts: [project.yml, Signing.xcconfig.example, src/App/GrokBotLocatorApp.swift, src/App/RootView.swift]
  key_links:
    - "project.yml globs src/ into the app target and tests/ into target GrokBotLocatorTests"
    - "project.yml configFiles point at Signing.xcconfig for Debug and Release"
    - "GrokBotLocatorApp presents RootView"
---

<objective>
Create the XcodeGen spec, signing wiring, and a launchable SwiftUI skeleton.
No location, no networking, no credentials — later plans build on this project.
</objective>

<context>
.planning/ARCHITECTURE.md (law: pins, Forbidden list, Smoke section), .planning/PROJECT.md,
.gitignore (already ignores Signing.xcconfig).
Greenfield — src/ and tests/ do not exist. Present: Xcode 26.6, Swift 6.3.3, xcodegen 2.46.0.
</context>

<tasks>
<task type="auto">
  <name>Task 1: project.yml and Signing.xcconfig.example</name>
  <files>project.yml, Signing.xcconfig.example</files>
  <action>XcodeGen 2.46.0 spec. name: GrokBotLocator; options.deploymentTarget.iOS "26.0". Never add a `packages:` key — zero third-party dependencies is law.
App target GrokBotLocator: type application, platform iOS, sources [src]; settings base SWIFT_VERSION 6.0, TARGETED_DEVICE_FAMILY "1", GENERATE_INFOPLIST_FILE YES, INFOPLIST_KEY_UISupportedInterfaceOrientations "UIInterfaceOrientationPortrait", INFOPLIST_KEY_CFBundleDisplayName GrokBotLocator; configFiles Debug and Release both Signing.xcconfig.
PRODUCT_BUNDLE_IDENTIFIER and DEVELOPMENT_TEAM must not appear in project.yml — they come only from that xcconfig.
Test target GrokBotLocatorTests: type bundle.unit-test, sources [tests], dependencies [target: GrokBotLocator], SWIFT_VERSION 6.0.
schemes: one named GrokBotLocator that builds the app target and tests GrokBotLocatorTests.
Create tests/ with a .gitkeep so the glob resolves before plan 01-02 adds files.
Signing.xcconfig.example: commented placeholder lines for both keys, no real values.</action>
  <verify>`xcodegen generate` exits 0 and creates GrokBotLocator.xcodeproj; `grep -rniE 'DEVELOPMENT_TEAM|PRODUCT_BUNDLE_IDENTIFIER' project.yml` finds nothing.</verify>
  <done>Project generates with both targets and the scheme; no signing identifiers tracked.</done>
</task>

<task type="auto">
  <name>Task 2: App skeleton</name>
  <files>src/App/GrokBotLocatorApp.swift, src/App/RootView.swift</files>
  <action>`@main struct GrokBotLocatorApp: App` with a single WindowGroup showing RootView. RootView: a NavigationStack containing a .font(.body) placeholder Text and a navigationTitle — plan 01-05 replaces its body with the settings screen, so keep it trivial. Import SwiftUI only: no CoreLocation, no URLSession, no credential-shaped literals. Must compile clean in Swift 6 language mode with no concurrency warnings.</action>
  <verify>`xcodebuild -scheme GrokBotLocator -destination 'platform=iOS Simulator,name=iPhone 17' -derivedDataPath build/dd-app build` succeeds with no warnings.</verify>
  <done>App target builds and launches portrait in an iOS 26 simulator.</done>
</task>
</tasks>
