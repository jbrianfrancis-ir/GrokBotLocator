---
phase: 02-manual-ping
plan: 11
wave: 11
depends_on: [02-10]
files_modified: [src/Settings/SettingsView.swift]
autonomous: true
requirements: [REQ-11, REQ-12]
must_haves:
  truths:
    - "Settings shows a Test connection control with a >= 60pt tap target whose word changes while a test runs and which is disabled meanwhile"
    - "The report renders on screen as symbol + word + colour plus the exact HTTP status and the exact response body — no alert, no toast, no lineLimit"
    - "A wrong sender key shows a visible 401 or 403 on screen with its body (REQ-11's acceptance)"
    - "The sender key field's footnote tells the user to type the whole header value, because the app sends it verbatim"
    - "At AX5 in light and dark the report wraps instead of clipping, and the stored key is still never re-rendered (D-10)"
  artifacts: [src/Settings/SettingsView.swift]
  key_links:
    - "SettingsView renders SettingsModel.connectionReport through the existing statusBadge helper"
---

<objective>
Put 02-10's report on the settings screen in DESIGN.md's vocabulary, and close REQ-11's
acceptance with a real wrong key against a real endpoint.
</objective>

<context>
.planning/DESIGN.md — outcome always symbol + word + colour; errors in body-size text on
screen, never a transient toast, never a bare status code; tap targets >= 60pt; nothing below
17pt; Dynamic Type to AX5 with no clipping; light and dark both required. D-10: the stored
sender key is never re-rendered.
From 02-10: `SettingsModel.ConnectionReport`, `isTesting`, `testConnection()`.
Existing: src/Settings/SettingsView.swift — its `statusBadge(symbolName:text:pair:)` helper is
the symbol+word+colour pattern to reuse, and the comment at its Clear button explains why the
`.frame` and `.contentShape` must live inside the Button's label.
Gate: ./scripts/smoke.sh.
</context>

<tasks>
<task type="auto">
  <name>Task 1: Test connection control and report</name>
  <files>src/Settings/SettingsView.swift</files>
  <action>Below the existing Save/Clear group, inside the same VStack, add a Test connection control built like the Clear button: a `Button` whose label is `Text(model.isTesting ? "Testing…" : "Test connection")` at `.dsFont(.body)` with `.frame(maxWidth: .infinity, minHeight: DSMetrics.minTapTarget)` and `.contentShape(Rectangle())` INSIDE the label (the existing comment explains why outside does not work), `.accessibilityLabel("Test connection")`, `.disabled(model.isTesting)`, action `Task { await model.testConnection() }`.
Render `model.connectionReport` when non-nil, directly below that control — never an `alert`, never a toast:
- headline through the existing `statusBadge`, with `checkmark.circle.fill` + `DSPalette.success` when `succeeded` and `exclamationmark.triangle.fill` + `DSPalette.failure` otherwise;
- when `statusCode` is non-nil, `Text("HTTP \(code)")` at `.dsFont(.secondary)`;
- when `responseBody` is non-nil, the body verbatim at `.dsFont(.secondary)`, `.textSelection(.enabled)`, and NO `lineLimit` so it wraps at AX5 instead of clipping. When the body is empty, render `Text("(empty response body)")` — a rendering note, not a substituted value;
- wrap status + body in one `.accessibilityElement(children: .combine)` so VoiceOver reads the whole answer as one announcement.
Give the "Sender key" `CredentialField` a `footnote`: "Type the whole header value the routine expects, for example Bearer abc123 — it is sent exactly as typed." Do not add a reveal control and do not render `model.senderKey` anywhere (D-10).</action>
  <verify>`./scripts/smoke.sh` exits 0; `grep -c 'Test connection' src/Settings/SettingsView.swift` is non-zero AND `grep -c 'connectionReport' src/Settings/SettingsView.swift` is non-zero — presence assertions, because the regression grep below matches nothing at HEAD and would pass before a line of this task is written; and `grep -nE '\.alert\(|Toast|lineLimit\(' src/Settings/SettingsView.swift` finds nothing (exit 1).</verify>
  <falsify>Add `.lineLimit(1)` to the response-body `Text`: the grep matches and exits 0. Revert.</falsify>
  <done>Status and body on screen, in the design system's vocabulary, wrapping at any text size.</done>
</task>

<task type="auto">
  <name>Task 2: AX5 previews and the on-screen 401</name>
  <files>src/Settings/SettingsView.swift</files>
  <action>`connectionReport` is `private(set)`, so a preview cannot assign one: add a private preview-only `StubPingSending` struct in this file returning a caller-set `PingAttempt`, give `previewModel(hasStoredKey:)` an optional sender parameter, and wrap the preview body in a tiny view that calls `.task { await model.testConnection() }` so the report exists by the time the canvas draws.
Add three `#Preview`s on that path, all with a `.permanentFailure` attempt carrying statusCode 401 and a multi-line body: one at default size, one at `.accessibility5`, and one dark at `.accessibility5`. Previews only — no production behaviour changes in this task.</action>
  <verify><human-check>On a booted iOS 26 simulator with the app installed: put a deliberately wrong sender key in Settings against a real endpoint (the Grok routine, or any HTTPS endpoint that 401s), tap Test connection, and confirm (a) the button reads "Testing…" while it runs, (b) the result appears on screen as a red triangle + sentence naming the code, with an `HTTP 401` (or 403) line and the response body below it, (c) no alert or toast appears, (d) nothing anywhere shows the stored key. Then drive `xcrun simctl ui <udid> content_size accessibility-extra-extra-extra-large` and repeat in `appearance light` and `appearance dark`: the sentence and the body wrap, nothing clips or overlaps.</human-check></verify>
  <falsify>Omitted deliberately: this task's verify is a human check, which the plan format exempts from a negative control.</falsify>
  <done>REQ-11's acceptance is closed by a visible 401 on a real screen, at AX5, in both appearances.</done>
</task>
</tasks>
