---
phase: 02-manual-ping
plan: 10
wave: 10
depends_on: [02-08, 02-09]
files_modified: [src/Settings/SettingsModel.swift, tests/SettingsModelTests.swift]
autonomous: true
requirements: [REQ-11]
must_haves:
  truths:
    - "Settings can send a real ping through the same PingSender the button uses — not a separate code path"
    - "The report carries the exact HTTP status code and the exact response body, unmodified and untruncated; an empty body stays an empty string and nil stays nil"
    - "A 401 or 403 attempt produces a report whose headline names the code — never a silent success and never an empty message"
    - "A test in flight is observable and a second call while it runs is ignored"
    - "testConnection never touches `status`, which belongs to save and clear"
    - "Every pre-existing SettingsModel test still passes — the new dependency is optional and defaults to nil"
  artifacts: [src/Settings/SettingsModel.swift, tests/SettingsModelTests.swift]
  key_links:
    - "SettingsModel takes a PingSending? by injection; tests pass a fake and the app passes the real PingSender"
---

<objective>
REQ-11: prove the configured URL, key and header actually work, from the screen where they were
typed, by showing exactly what the webhook answered.
</objective>

<context>
.planning/REQUIREMENTS.md REQ-11 — a wrong key shows a visible 401/403, never a silent failure.
.planning/DESIGN.md — outcome is always symbol + word + colour; errors in body-size text on
screen, never a transient toast, never a bare status code; tap targets >= 60pt; nothing below
17pt. D-10: the stored sender key is never re-rendered.
From 02-08: src/Ping/PingSender.swift (PingSending, PingAttempt). From 02-03:
src/Core/Transport/PingTransport.swift (PingDisposition).
Existing: src/Settings/SettingsModel.swift, tests/SettingsModelTests.swift. Plan 02-11 renders
the report on screen.
A test connection is a diagnostic, not a logged ping: it does NOT write to the ping history.
`depends_on` carries two edges: 02-08 is a consumption edge (PingSending, PingAttempt), 02-09
is the phase's build edge — concurrent smoke.sh runs would race on the generated project, the
single target over all of src/, and the shared derived-data dir.
Gate: ./scripts/smoke.sh. Swift Testing, never XCTest.
</context>

<tasks>
<task type="auto">
  <name>Task 1: testConnection on SettingsModel</name>
  <files>src/Settings/SettingsModel.swift</files>
  <action>Add a second init parameter `sender: PingSending? = nil` stored as `private let sender`. The default keeps every existing call site and test compiling unchanged.
Add `struct ConnectionReport: Equatable, Sendable { let succeeded: Bool; let headline: String; let statusCode: Int?; let responseBody: String? }` (nested in SettingsModel) and `private(set) var connectionReport: ConnectionReport?`, `private(set) var isTesting = false`.
`func testConnection() async`:
- `guard !isTesting else { return }`; `guard let sender else { connectionReport = ConnectionReport(succeeded: false, headline: "Test connection is unavailable in this build.", statusCode: nil, responseBody: nil); return }`;
- `isTesting = true`; `defer { isTesting = false }`;
- `let attempt = await sender.send(label: "Test connection")` — this screen has no label field, so the literal is what distinguishes a diagnostic ping at the receiving end;
- build the report: `.sent` → succeeded true, headline "The webhook accepted the test ping."; `.permanentFailure(let r)` / `.retryable(let r)` → succeeded false, headline `r`. Carry `attempt.statusCode` and `attempt.responseBody` through verbatim — do not trim, truncate, or replace an empty body with a placeholder word; nil stays nil.
Never print, log, or interpolate a credential. Do not touch `status`, which belongs to save/clear.</action>
  <verify>`./scripts/smoke.sh` exits 0; `grep -c 'func testConnection' src/Settings/SettingsModel.swift` prints 1 AND `grep -c 'ConnectionReport' src/Settings/SettingsModel.swift` is non-zero — presence assertions, because the regression grep below matches nothing at HEAD and would pass before a line of this task is written; and `grep -nE 'print\(|NSLog|os_log|prefix\(|truncat' src/Settings/SettingsModel.swift` finds nothing (exit 1).</verify>
  <falsify>Change the report to `responseBody: attempt.responseBody.map { String($0.prefix(100)) }`: the grep matches `prefix(` and exits 0. Revert.</falsify>
  <done>A report that carries the webhook's exact answer, with the dependency optional.</done>
</task>

<task type="auto">
  <name>Task 2: testConnection tests</name>
  <files>tests/SettingsModelTests.swift</files>
  <action>Extend the existing `SettingsModelTests` suite (keep every current case unchanged) with an in-file fake `PingSending` returning a caller-set `PingAttempt` and counting calls, plus one that suspends so the in-flight window is observable.
Declare these seven tests with exactly these names — the verify below reads them out of the runner log, because `Suite SettingsModelTests passed` already appears in the log at HEAD and would pass before a single new case is written:
- `noSenderProducesAnUnavailableReport` — a model built with no sender produces a report with `succeeded == false` and a non-empty headline, and never crashes;
- `sentAttemptCarriesStatusAndBody` — a `.sent` attempt with statusCode 200 and body `{"ok":true}` gives `succeeded == true`, `statusCode == 200`, `responseBody == "{\"ok\":true}"`;
- `unauthorizedAttemptShowsItsCodeAndBody` — a `.permanentFailure` attempt with statusCode 401 and body `unauthorized` gives `succeeded == false`, `statusCode == 401`, `responseBody == "unauthorized"`, and the headline contains "401" — REQ-11's acceptance;
- `aLongResponseBodyIsNotTruncated` — a body of 5000 characters comes back with `responseBody?.count == 5000`;
- `anEmptyResponseBodyStaysEmpty` — an empty body comes back as `""`, not nil and not a placeholder;
- `aSecondTestWhileOneIsInFlightIsIgnored` — while the suspended sender is pending, `isTesting` is true and a second `testConnection()` leaves the call count at 1;
- `testConnectionLeavesSaveStatusAlone` — `testConnection()` does not change `model.status`; save and clear own that field.</action>
  <verify>`./scripts/smoke.sh` exits 0 AND its log contains `Suite SettingsModelTests passed` AND all seven of `noSenderProducesAnUnavailableReport`, `sentAttemptCarriesStatusAndBody`, `unauthorizedAttemptShowsItsCodeAndBody`, `aLongResponseBodyIsNotTruncated`, `anEmptyResponseBodyStaysEmpty`, `aSecondTestWhileOneIsInFlightIsIgnored`, `testConnectionLeavesSaveStatusAlone`. The suite-passed line alone is not enough: that suite exists and is green at HEAD, so it would match before any of these cases is written.</verify>
  <falsify>Make `testConnection` assign `status = .saved` on success: `testConnectionLeavesSaveStatusAlone` fails and the log shows `Suite SettingsModelTests failed`. Revert.</falsify>
  <done>The exact status and body are proven to survive the model, including a long body and an empty one.</done>
</task>
</tasks>
