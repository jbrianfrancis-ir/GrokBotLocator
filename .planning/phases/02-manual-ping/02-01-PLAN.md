---
phase: 02-manual-ping
plan: 01
wave: 1
depends_on: []
files_modified: [tests/DynamicTypeScalingTests.swift]
autonomous: true
requirements: [REQ-12]
must_haves:
  truths:
    - "The rendered height order of all four tokens is asserted by a test at BOTH .large and .accessibility5, so any later curve or size change fails the gate instead of drifting silently"
    - "At .large the asserted order is strictly ascending: secondary < body < actionLabel < screenTitle"
    - "At .accessibility5 the test asserts the order the tokens actually render in — including the actionLabel-over-screenTitle inversion, which is accepted shipped behavior per the 2026-09-10 15:30 decision, not a defect left open"
    - "DSTypography.swift is not edited: the four curves and the 17/20/28/34 sizes are exactly what phase 01 shipped"
    - "The existing >=1.5x default-to-AX5 scaling test and its fixed-size negative control both still pass"
  artifacts: [tests/DynamicTypeScalingTests.swift]
  key_links:
    - "DynamicTypeScalingTests is the only place the rendered order of the four type tokens is asserted"
---

<objective>
Ship the relative-order tripwire the PR #1 review assigned to phase 02: a test that pins how the
four type tokens actually render at both sizes, so a later curve or size change fails the gate
instead of drifting silently. No token is changed.
</objective>

<context>
.planning/DECISIONS.md — **2026-09-10 15:30 checkpoint-decision is the authority for this plan.**
The human was asked whether to force the AX5 order and answered, verbatim, "leave the curves
alone, don't gate the phase". So: DSTypography's curves are untouched, the AX5 inversion where
`actionLabel` (79) renders larger than `screenTitle` (70) is accepted as shipped behavior, and
this plan ships only the test. Do not edit src/Core/DesignSystem/DSTypography.swift — not the
curves, not the sizes, not to "improve" the order. A plan that changes it contradicts a recorded
human decision.
.planning/DECISIONS.md — 2026-09-10 14:26 review-refute carries the measurements the test will
be compared against: at AX5 the four tokens render secondary 59, body 67, screenTitle 70,
actionLabel 79. Treat those as orientation, not as the expectations: measure first, assert what
the harness reports.
.planning/DESIGN.md — type scale 17/20/28/34, nothing below 17pt, Dynamic Type to AX5. It pins
sizes, NOT which `Font.TextStyle` curve each token scales along, which is why the 15:30 option
is inside the law rather than an amendment to it.
src/Core/DesignSystem/DSTypography.swift is READ-ONLY context here (four tokens on four curves:
.body, .callout, .title2, .largeTitle). tests/DynamicTypeScalingTests.swift is the file this plan
changes; its private `height(of:at:)` ImageRenderer helper is the measurement harness to reuse.
Gate: ./scripts/smoke.sh. Swift Testing, never XCTest.
</context>

<tasks>
<task type="auto">
  <name>Task 1: pin the rendered order at both sizes</name>
  <files>tests/DynamicTypeScalingTests.swift</files>
  <action>Add to the existing `@MainActor @Suite struct DynamicTypeScalingTests`, reusing its private `height(of:at:)` helper and changing no token:

1. `@Test func tokenOrderAtDefaultSize()` — render `Text("Ag").dsFont(style)` for all four styles at `.large` and `#expect` strict ascending order secondary < body < actionLabel < screenTitle, one expectation per adjacent pair, each failure message naming the pair and both measured heights.

2. `@Test func tokenOrderAtAX5IsPinned()` — the same four measurements at `.accessibility5`, asserting the order the tokens ACTUALLY render in today. Do not assume DECISIONS' numbers: measure first, then write the assertions to match what the harness reports, one expectation per adjacent pair in the measured order. Put the four measured heights in a comment above the test with the date, and one line saying these expectations describe the status quo deliberately — this is a tripwire, so a later curve or size change fails here rather than drifting unnoticed — and that the `actionLabel` > `screenTitle` inversion it encodes is accepted behavior per the 2026-09-10 15:30 decision, not an open defect.

3. `@Test func tokenHeightsAtBothSizes()` — measure all four tokens at `.large` and `.accessibility5`, `#expect` every height is > 0, and emit all eight numbers on one line with `print("token heights — large: secondary \(…) body \(…) actionLabel \(…) screenTitle \(…); AX5: …")`, so the numbers behind the pinned expectations are in the smoke log rather than only in a comment. Use `print`, not `Issue.record` — recording an issue in Swift Testing fails the test. ARCHITECTURE's no-logging rule and smoke.sh's guard both scope to `src/`, so a `print` in a test file is fine.

Keep both existing tests untouched — they are this suite's negative control.</action>
  <verify>`./scripts/smoke.sh` exits 0 AND its log contains all three of `tokenOrderAtDefaultSize`, `tokenOrderAtAX5IsPinned` and `tokenHeightsAtBothSizes`, AND contains the literal `token heights —`. Also DSTypography.swift still carries phase 01's four curves and four sizes, asserted by content rather than by git state: `grep -cF 'case .body: return .body'`, `grep -cF 'case .secondary: return .callout'`, `grep -cF 'case .actionLabel: return .title2'`, `grep -cF 'case .screenTitle: return .largeTitle'`, `grep -cF 'case .body: return 20'`, `grep -cF 'case .secondary: return 17'`, `grep -cF 'case .actionLabel: return 28'` and `grep -cF 'case .screenTitle: return 34'` each print exactly 1 on that file (each string is unique to it — the weight block returns .regular/.semibold/.bold, so none of these can match twice). This plan ships no source change, and an altered curve or size means the 15:30 decision was overstepped. Assert content, NOT `git diff --quiet <path>`: that compares the working tree to the index, so an edit the executor staged or committed — which is what executors do — passes it clean.</verify>
  <falsify>Change `.screenTitle`'s `relativeTo` to `.body` in DSTypography.swift. That must INVERT A PINNED PAIR, not merely move a token: `.body`'s ~3.1x factor takes screenTitle from ~70pt to ~106pt, above actionLabel's 79pt, so the pinned `screenTitle < actionLabel` expectation in `tokenOrderAtAX5IsPinned` fails by name — and the `case .screenTitle: return .largeTitle` count drops to 0 as a second effect, failing the content assertion no matter whether the edit is unstaged, staged or committed. Do not reach for a mutation that shrinks the already-smallest token (`.secondary` → `.largeTitle` takes it to ~27pt): every pairwise `<` stays true and the test stays green, which would prove nothing. Revert with `git checkout -- src/Core/DesignSystem/DSTypography.swift` and confirm smoke.sh is green again. Worth noting what this control demonstrates: the mutation is exactly the curve change option (a) of the 2026-09-10 15:30 decision would have made, so watching it go red is watching the tripwire do the job it exists for.</falsify>
  <done>The order is a test at both sizes, the eight heights are in the log, and DSTypography is byte-for-byte what phase 01 shipped.</done>
</task>

<task type="auto">
  <name>Task 2: record the tripwire's baseline where the next planner will find it</name>
  <files>tests/DynamicTypeScalingTests.swift</files>
  <action>At the top of the suite, extend the existing doc comment with three lines: that `tokenOrderAtAX5IsPinned` encodes measured behavior rather than intended hierarchy; that the measured AX5 inversion (`actionLabel` above `screenTitle`) is accepted per .planning/DECISIONS.md 2026-09-10 15:30; and that a future phase wanting strict order at every size must change a curve in DSTypography and re-run REQ-12's Accessibility Inspector audit on every screen, because the order test failing is the signal that the change has a reach beyond one file. No behaviour change; this is the note that stops the next reader treating a red tripwire as a bug in the test.</action>
  <verify>`./scripts/smoke.sh` exits 0, and `grep -c '15:30' tests/DynamicTypeScalingTests.swift` prints at least 1.</verify>
  <falsify>Delete the `15:30` reference from the comment: the grep prints 0. Revert.</falsify>
  <done>The next planner reads why the AX5 expectations look wrong before deciding they are.</done>
</task>
</tasks>
