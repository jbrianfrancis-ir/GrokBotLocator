---
phase: 02-manual-ping
plan: 13
wave: 13
depends_on: [02-12]
files_modified: [src/App/GrokBotLocatorApp.swift, src/App/RootView.swift]
autonomous: true
requirements: [REQ-02, REQ-03, REQ-04, REQ-10, REQ-12, SC-01]
must_haves:
  truths:
    - "The app launches into the ping screen, and Settings is one tap away from its header bar and back again"
    - "KeychainCredentialStore, URLSessionPingTransport, CoreLocationFixProvider, UserDefaultsPingLabelStore and UnqueuedPingSink are each constructed exactly once, in the app entry point and nowhere else"
    - "SettingsModel receives the same PingSender the ping button uses, so Test connection and I'm here cannot diverge"
    - "A manual ping at When In Use delivers the exact four-key JSON to a real endpoint and shows Sent in under 10 s (REQ-02, SC-01)"
    - "One success and one forced 401 give two history rows with distinct outcomes, the failed one carrying its reason (REQ-04)"
    - "A typed label survives a force-quit and relaunch (REQ-03)"
    - "At When In Use the button works and the screen names what Always would add; denying location leaves a guidance sentence and no history row (REQ-10)"
    - "PingButton's in-flight state, PingOutcomeRow at AX5 in both appearances, and .dsChrome()'s opaque fallback are all confirmed on screen — the three checks phase 01 deferred"
    - "REQ-12's acceptance is run on the ping screen: at AX5 in light and dark the Accessibility Inspector audit reports zero contrast findings and zero hit-target findings, the title wraps rather than truncating, and nothing clips or overlaps"
    - "The same audit is re-run on the settings screen because plan 02-11 added a control and a report to it — that change alone makes phase 01's 2026-09-10 closure of REQ-12 no longer a description of the shipped screen"
  artifacts: [src/App/GrokBotLocatorApp.swift, src/App/RootView.swift]
  key_links:
    - "GrokBotLocatorApp is the only file naming a concrete store, transport or location provider"
    - "RootView registers .navigationDestination(for: SettingsRoute.self) so PingHomeView's header link reaches SettingsView"
---

<objective>
Wire the phase together and prove it on screen: the app opens on "I'm here", and the three
acceptance checks phase 01 deferred get closed where real screens use the components.
</objective>

<context>
.planning/ROADMAP.md "Carried into phase 02 from phase 01" and
.planning/phases/01-foundation-design-system/VERIFICATION.md — the three `[→]` deferred
entries this plan must close, and the standard of evidence the earlier ones were held to.
.planning/ARCHITECTURE.md — one construction site for concrete dependencies; fail fast with no
fallback endpoint. .planning/DESIGN.md. .planning/REQUIREMENTS.md REQ-02, REQ-03, REQ-04,
REQ-10, REQ-12, SC-01.
Existing: src/App/GrokBotLocatorApp.swift (today's single-store pattern),
src/App/RootView.swift. From 02-12: src/Ping/PingHomeView.swift, `SettingsRoute`.
Gate: ./scripts/smoke.sh.
</context>

<tasks>
<task type="auto">
  <name>Task 1: composition root and root navigation</name>
  <files>src/App/GrokBotLocatorApp.swift, src/App/RootView.swift</files>
  <action>In `GrokBotLocatorApp`, build the object graph once. A `@State` property's default-value expression cannot reference another property of the same struct, so sharing one `PingSender` between the two models REQUIRES an explicit `init()`: build all of it as locals there, then assign through the underscored projections — `init() { let store = KeychainCredentialStore(); let fixes = CoreLocationFixProvider(); let sender = PingSender(credentials: store, fixes: fixes, transport: URLSessionPingTransport(), pending: UnqueuedPingSink()); _pingModel = State(wrappedValue: PingModel(sender: sender, labelStore: UserDefaultsPingLabelStore(), fixes: fixes)); _settingsModel = State(wrappedValue: SettingsModel(store: store, sender: sender)) }`. Writing them as two independent `@State` initialisers cannot compile and cannot share the sender — that is the whole reason this init exists. The argument labels above are the ones 02-08, 02-09 and 02-10 pin. Keep the file's existing comment style and extend its header comment to say this is still the only place a concrete Keychain, URLSession, CoreLocation or UserDefaults type is named.
`RootView` takes both models, and its `NavigationStack` now wraps `PingHomeView(model: pingModel)` with `.navigationDestination(for: SettingsRoute.self) { _ in SettingsView(model: settingsModel) }`. Keep `.navigationBarTitleDisplayMode(.inline)` and add no `navigationTitle` — the reason is in RootView's existing comment, and PingHomeView's `Text("Send a ping")` in its SCROLLING CONTENT is the home screen's only title. Do not put a title in PingHomeView's chrome bar and do not add one here: 02-12 deliberately leaves that bar text-free, because text over a translucent surface with content scrolling beneath it has no fixed background to measure DESIGN.md's 7:1 contrast floor against, and a screen title at AX5 is too wide for the bar in portrait.
Do not construct any of those concrete types anywhere else in src/.</action>
  <verify>`./scripts/smoke.sh` exits 0, and for each of the five constructors the truth names — `KeychainCredentialStore()`, `CoreLocationFixProvider()`, `URLSessionPingTransport()`, `UnqueuedPingSink()`, `UserDefaultsPingLabelStore()` — `grep -rc '<constructor>' src/ --include='*.swift'` reports a non-zero count for `src/App/GrokBotLocatorApp.swift` and zero for every other file (preview-only fakes inside `#Preview` helpers use none of these types).</verify>
  <falsify>Add `let t = URLSessionPingTransport()` to src/Ping/PingHomeView.swift: the second grep now reports a non-zero count for a second file. Revert.</falsify>
  <done>One construction site; the app opens on the ping screen with Settings reachable and returnable.</done>
</task>

<task type="auto">
  <name>Task 2: close the three checks phase 01 deferred</name>
  <files>none — verification only; a failure is fixed in the file owning the component</files>
  <action>No code change. This task exists to carry the three carried-forward acceptance checks, now that the components have real call sites. If any check fails, fix it in the file that owns the component and re-run `./scripts/smoke.sh`.</action>
  <verify><human-check>On a booted iOS 26 simulator with the app installed and credentials saved:
(a) **PingButton in flight** — point the webhook URL at an unroutable HTTPS host (e.g. `https://10.255.255.1/webhook`) so the 10 s timeout holds the window open. Tap "I'm here": a spinner appears beside the word "Pinging…", the button does not respond to a second tap, and with VoiceOver on it reads "I'm here, In progress". Then the attempt ends with a failure row carrying its reason.
(b) **PingOutcomeRow at AX5** — with one successful ping and one forced 401 in the list, run `xcrun simctl ui <udid> content_size accessibility-extra-extra-extra-large` and check both `appearance light` and `appearance dark`: each row shows its SF Symbol, its word, and its colour together; the failure reason wraps to as many lines as it needs; coordinates and timestamp stay readable; nothing clips or overlaps.
(c) **DSChrome opaque fallback** — with Reduce Transparency OFF, scroll the history under the header bar and confirm content is visibly blurred through it (the negative control: this is the `.thinMaterial` branch). Turn Reduce Transparency ON in Settings ▸ Accessibility ▸ Display & Text Size and confirm the bar becomes a flat opaque fill with nothing showing through. Repeat for the other branch with `xcrun simctl ui <udid> increase_contrast enabled`.</human-check></verify>
  <falsify>Omitted deliberately: this task's verify is a human check, which the plan format exempts from a negative control.</falsify>
  <done>All three `[→]` entries in phase 01's VERIFICATION are answered on screen, with the DSChrome check carrying its own negative control.</done>
</task>

<task type="auto">
  <name>Task 3: close the phase's own acceptance checks</name>
  <files>none — verification only; a failure is fixed in the file owning the component</files>
  <action>No code change. Carries the end-to-end checks no unit test can make. Fix and re-run smoke.sh on any failure.</action>
  <verify><human-check>With the app installed and pointed at an HTTPS endpoint that records the raw request body (the Grok routine itself, or a request-inspection URL you control):
(a) **REQ-02 + SC-01** — type the label "Gallipoli", tap "I'm here" on a normal connection, and time it: a `Sent` row appears in under 10 s. The endpoint's recorded body is exactly `{"lat":<number>,"lng":<number>,"accuracy_m":<number>,"label":"Gallipoli"}` — four keys, `lat`/`lng` as bare numbers with no quotes. Key order is part of the check here, which it could not have been under a `JSONEncoder`-built body: plan 02-02 composes the object itself precisely so the order ARCHITECTURE.md names is deterministic, and this is the one place a human sees that on the wire. The header the credential names carries the key exactly as typed. Record the measured seconds.
(b) **REQ-04** — change the sender key to a wrong value, ping again for a 401, restore the key: the list now holds two rows with distinct outcomes, and the failed row shows its reason sentence.
(c) **REQ-03** — with "Gallipoli" in the field, force-quit the app (swipe it away, or `xcrun simctl terminate <udid> <bundle-id>`) and relaunch: the field still reads "Gallipoli".
(d) **REQ-10** — with the app's location set to "While Using the App", the button still sends and the screen shows the notice naming what Always would add. Then set location to "Never" and tap: a guidance sentence appears on screen saying what to do, and NO history row is added. Restore the setting.
(e) **VoiceOver announcement** — with VoiceOver on, a completed ping is spoken, not only drawn.</human-check></verify>
  <falsify>Omitted deliberately: this task's verify is a human check, which the plan format exempts from a negative control.</falsify>
  <done>REQ-02, REQ-03, REQ-04, REQ-10 and SC-01 each closed against a real endpoint on a real screen.</done>
</task>

<task type="auto">
  <name>Task 4: REQ-12 audit on both screens at AX5</name>
  <files>none — verification only; a failure is fixed in the file owning the offending view</files>
  <action>No code change. REQ-12 says every screen meets DESIGN.md and its acceptance is an Accessibility Inspector audit reporting no contrast or hit-target failures at AX5 in both appearances. Phase 01 closed that for SettingsView on 2026-09-10, and plan 02-11 moved it by adding a control and a report to that screen — that is the sole reason for the re-audit, and it stands on its own: the 2026-09-10 15:30 decision left DSTypography's curves untouched, so no type token changed anywhere in this phase. PingHomeView has never been audited at all. Both screens are audited here.
Run the orchestrator side first: `xcrun simctl ui <udid> content_size accessibility-extra-extra-extra-large`, then `appearance light` and `appearance dark` for each pass, with the app rebuilt from HEAD and reinstalled (phase 01 learned that an install sharing a timestamp minute with the last edit cannot be proven current). Fix any finding in the view that owns it and re-run `./scripts/smoke.sh`.</action>
  <verify><human-check>With the app installed from a build of HEAD and `content_size accessibility-extra-extra-extra-large` set:
(a) **Ping screen, light then dark** — run the Accessibility Inspector audit on PingHomeView with at least one sent and one failed row present. Expect zero contrast findings and zero hit-target findings. Confirm by eye as well: the "Send a ping" title wraps across lines rather than truncating or running off the edge; the label field, its footnote, the authorization notice, every row's reason, the coordinates and the timestamp all wrap with nothing clipped or overlapping; the gear in the chrome bar is still a >= 60pt target; the "I'm here" button stays full-width in the bottom third and its 28pt label is not cut off.
(b) **Settings screen, light then dark** — re-run the same audit on SettingsView, scrolling to bring the Save / Clear / Test connection group on screen. Expect zero contrast findings and zero hit-target findings, the Test connection report wrapping, and the sender key still showing only its saved indicator. This re-audit replaces phase 01's 2026-09-10 closure.
(c) Both screens are audited against phase 01's type tokens unchanged (2026-09-10 15:30), so a contrast or hit-target finding here is about something this phase built, not about a curve that moved. Note in the result that `actionLabel` renders larger than `screenTitle` at AX5 — accepted behavior per that decision and pinned by 02-01's tripwire — so neither you nor the auditor files it as a new defect.
Record the verdict per screen per appearance, not as a single "pass".</human-check></verify>
  <falsify>Omitted deliberately: this task's verify is a human check, which the plan format exempts from a negative control.</falsify>
  <done>REQ-12 is closed for the ping screen and re-closed for the settings screen, at AX5, in both appearances.</done>
</task>
</tasks>
