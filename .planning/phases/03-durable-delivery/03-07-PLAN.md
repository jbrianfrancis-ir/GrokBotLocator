---
phase: 03-durable-delivery
plan: 07
wave: 3
depends_on: [03-04]
files_modified: [src/Ping/PingHistory.swift, src/Ping/PingModel.swift, tests/PingHistoryTests.swift, tests/PingModelTests.swift]
autonomous: true
requirements: [REQ-04, REQ-05]
must_haves:
  truths:
    - "A retryable send that the sink accepted shows as Queued, not Failed — the arm phase 02 wired as a no-op is now live"
    - "A Queued row carries the reason it is waiting, so the user knows why it has not gone yet"
    - "A later delivery UPDATES the existing row in place — same id, same position in the list, outcome flips Queued→Sent — rather than adding a second row for the same ping"
    - "A delivery update for an id the log has never seen INSERTS a row, so a ping queued before a relaunch still appears in a fresh session's history"
    - "Applying an update never grows the log past its capacity and never reorders rows that did not change"
    - "A drain notice can actually reach the screen: PingModel exposes an entry point that sets `guidance`, so a coordinator outside the file can put a sentence on screen without widening `private(set) var guidance`"
    - "Applying updates silently leaves `lastAttempt` exactly as it was: no badge, no VoiceOver announcement. A relaunch is not a tap, and announcing an outcome for an attempt the user never made is what this forbids"
    - "An empty update list announces nothing under either mode — a drain that delivered nothing is not an event"
  artifacts: [src/Ping/PingHistory.swift, src/Ping/PingModel.swift]
  key_links:
    - "PingModel stamps the history row with PingAttempt.queuedID, so the queue entry and the row are the same ping"
    - "PingQueueDrain (03-09) emits PingDeliveryUpdate values; QueueDrainCoordinator (03-10) hands them to PingModel.apply(_:announcing:) — `announcing: true` for a drain the user is present for, `announcing: false` for launch hydration"
    - "QueueDrainCoordinator (03-10) calls PingModel.show(notice:) for a PingDrainReport notice — `guidance` stays `private(set)` and this is the only way in"
---

<objective>
Flip the arm phase 02 left dead: a retryable, successfully-queued ping reads Queued on screen.
And give the log an update channel so the same row can later flip to Sent without a duplicate.
</objective>

<context>
.planning/phases/02-manual-ping/VERIFICATION.md — "Retryable sends are `.failed` with a reason,
never `.queued`: `PendingPingSink` is a wired, tested no-op. Phase 03 swaps the sink AND flips
that arm in `PingModel.ping()`." This plan is the second half of that sentence.
src/Ping/PingModel.swift:91-100 — the `.retryable` arm and the comment marking it unreachable.
src/Ping/PingHistory.swift — `PingHistoryEntry` (already `Identifiable` with a `UUID`),
`PingHistoryLog` (newest-first, capacity 50, in-memory only and staying that way — the DURABLE
store is the queue file, not this).
src/Core/DesignSystem/PingOutcomeRow.swift — `PingOutcome.queued` already renders as clock +
"Queued" + `DSPalette.pending`, and `PingOutcomeRow` already draws a `reason` line; no view
work is needed for a Queued row to appear.
From 03-04: `PingAttempt.queuedID`.
Gate: SMOKE_DERIVED_DATA=build/dd-03-07 ./scripts/smoke.sh. Swift Testing, never XCTest.
</context>

<tasks>
<task type="auto">
  <name>Task 1: PingDeliveryUpdate and PingHistoryLog.apply</name>
  <files>src/Ping/PingHistory.swift</files>
  <action>Add `struct PingDeliveryUpdate: Sendable, Equatable` with exactly the fields a row needs to be either created or corrected: `let id: UUID`, `let timestamp: Date` (the FIX time), `let latitude: Double`, `let longitude: Double`, `let label: String`, `let outcome: PingOutcome`, `let reason: String?`. Rich enough to insert on its own, so a ping queued in a previous session can reach a fresh log without the log having to have seen it first.

Add `mutating func apply(_ update: PingDeliveryUpdate)` to `PingHistoryLog`: if an entry with that `id` exists, replace it AT ITS CURRENT INDEX with an entry built from the update (id preserved) — the row must not jump to the top when its outcome changes, because a list that reorders under the user is how a "flipped to Sent" becomes indistinguishable from a second ping. Otherwise `record` a new entry with that id, which already inserts newest-first and enforces `capacity`.
Keep the file's existing header comment true: this log is still in-memory and session-only.</action>
  <verify>`SMOKE_DERIVED_DATA=build/dd-03-07 ./scripts/smoke.sh` exits 0, and `grep -nE 'Codable|FileManager|UserDefaults' src/Ping/PingHistory.swift` finds nothing (exit 1) — the history is still session-only and the queue file is still the only durable store.</verify>
  <falsify>Add `: Codable` to `PingHistoryEntry`: the grep matches and exits 0. Revert.</falsify>
  <done>The log can be corrected in place, and is still not a second store.</done>
</task>

<task type="auto">
  <name>Task 2: the Queued arm, the notice entry point, and the one test that asserted the old arm</name>
  <files>src/Ping/PingModel.swift, tests/PingModelTests.swift</files>
  <action>One task, one commit: flipping the arm makes an existing named test fail, and a plan whose intermediate state is red cannot have a green gate, so the test change lands here rather than in Task 3.

In `ping()`, replace the `.retryable(let r)` arm's body: `outcome = .queued`, `reason = r`. Replace the "UNREACHABLE" comment with what is now true — by the time this arm is reached the sink has accepted the payload (`PingSender` downgrades a refused enqueue to `.permanentFailure` before returning), so `.queued` here means a ping something is actually holding, which is what makes ARCHITECTURE's "no ping silently dropped" hold in this direction too.

Stamp the recorded entry with the queue's identity: `PingHistoryEntry(id: attempt.queuedID ?? UUID(), timestamp: fix.timestamp, …)`. The `??` covers the sent and permanently-failed paths, which carry no id and need none.

Add `func apply(_ updates: [PingDeliveryUpdate], announcing: Bool)` forwarding each to `log.apply(_:)`. The parameter is NOT defaulted: both call sites must say which they mean, because the wrong one is silent in review and loud on the device.
- `announcing: true` — set `lastAttempt` to a fresh `PingAttemptFeedback` (next `attemptSequence`) for the LAST update only, so a queued ping flipping to Sent is announced (DESIGN.md: the outcome is announced, not just drawn) without narrating a ten-entry drain one row at a time.
- `announcing: false` — apply every update to the log and leave `lastAttempt` and `attemptSequence` UNTOUCHED. This is the launch-hydration mode, and it exists because `PingAttemptFeedback.spoken` (this file, line ~18) reads `outcome == .sent ? "Ping sent." : "Ping failed. " + (reason ?? "")`: hydrating a queued row would otherwise post "Ping failed. Waiting to send. It will go out at the next opportunity." to VoiceOver and light the badge, for a tap that never happened. A row restored from disk is a fact about the past, not an outcome.
- Either mode: an EMPTY `updates` array touches nothing at all — no feedback, no sequence bump. A drain that delivered nothing is not an event.
Say in the doc comment that only `.sent` and `.failed` updates ever reach the announcing path (03-09's drain emits no `.queued` updates — a still-waiting ping has not changed for the user), which is why `spoken`'s two-way split is adequate there and only there.

Add `func show(notice: String) { guidance = notice }` — the ONLY way anything outside this file can put a drain sentence on screen. `guidance` stays `private(set)`: widening it would let any caller clear or overwrite the authorization sentence the view already reconciles against. Document that a drain notice and an authorization notice are different things and this entry point does not touch `authorizationNotice`; the existing `guidance == authorizationNotice` de-duplication in `ping()` is unchanged and still runs on the ping path only.

Then fix the ONE existing test this flip falsifies — `aRetryableFailureWithAFixAlsoRecordsOneFailedEntry` at tests/PingModelTests.swift:194, which asserts `entries[0].outcome == .failed` for exactly this disposition and already carries the comment "Documented phase-02 mapping: phase 03 (REQ-05) turns this into `.queued`". INVERT it, do not delete it: rename it to `aRetryableFailureWithAFixRecordsOneQueuedEntry`, change the expectation to `.queued`, keep the reason assertion as it is, and replace the phase-02 comment with one saying the mapping is now live. It stays a test because the disposition→outcome mapping is exactly what must not drift back.</action>
  <verify>`SMOKE_DERIVED_DATA=build/dd-03-07 ./scripts/smoke.sh` exits 0, and `grep -n 'outcome = .failed' src/Ping/PingModel.swift | wc -l` is 1 (the `.permanentFailure` arm alone), and `grep -c 'aRetryableFailureWithAFixAlsoRecordsOneFailedEntry' tests/PingModelTests.swift` is 0 — the old name is gone rather than left beside the new one.</verify>
  <falsify>Set `outcome = .failed` in the `.retryable` arm as well: the count becomes 2 and the check fails. Revert.</falsify>
  <done>A queued ping reads Queued, a drain notice has a way onto the screen, and no test still asserts the old mapping.</done>
</task>

<task type="auto">
  <name>Task 3: tests for the update channel, the in-place flip, and the notice entry point</name>
  <files>tests/PingHistoryTests.swift, tests/PingModelTests.swift</files>
  <action>In `PingHistoryTests`:
- `@Test func applyingAnUpdateForAKnownIdReplacesTheRowInPlace()` — record three entries, apply an update for the MIDDLE one flipping `.queued` to `.sent` with `reason` nil; assert the log still has three entries, the updated one is still at index 1, its outcome is `.sent` and its reason nil.
- `@Test func applyingAnUpdateForAnUnknownIdInsertsANewestFirstRow()` — apply an update against an empty log; assert one entry, matching every field.
- `@Test func applyingUpdatesNeverExceedsCapacity()` — fill past `PingHistoryLog.capacity` with fresh ids via `apply`; assert `entries.count == PingHistoryLog.capacity`.
In `PingModelTests` (its `FakeSender` already replays a caller-set `PingAttempt`):
- `@Test func aRetryableAttemptRecordsAQueuedRowCarryingItsReason()` — attempt with a fix, `.retryable(reason: "The webhook is unavailable (HTTP 503). Try again in a moment.")` and a known `queuedID`; assert one row, `outcome == .queued`, `reason` equal to that sentence, and `id == queuedID`.
- `@Test func aQueuedRowFlipsToSentWhenTheDrainReportsIt()` — after the above, `apply([PingDeliveryUpdate(id: queuedID, …, outcome: .sent, reason: nil)], announcing: true)`; assert STILL one row, now `.sent` with nil reason, and that `lastAttempt?.outcome == .sent` so the flip is announced.
- `@Test func aDeliveredAttemptStillHasNoQueuedIdentity()` — a `.sent` attempt records a row whose id is not any queued id and whose outcome is `.sent`.
- `@Test func applyingSilentlyLeavesLastAttemptUntouched()` — from a fresh model (so `lastAttempt` is nil), `apply([<a .queued update>, <a .failed update>], announcing: false)`; assert both rows landed in the log AND `lastAttempt` is still nil. Then `ping()` once, capture `lastAttempt`, apply another batch with `announcing: false`, and assert `lastAttempt` is unchanged — including its `sequence`, so the counter did not move either. This is the test that stops a relaunch announcing "Ping failed" for a tap nobody made.
- `@Test func anEmptyUpdateListAnnouncesNothing()` — `apply([], announcing: true)` on a fresh model leaves `lastAttempt` nil and the log empty.
- `@Test func showNoticePutsASentenceOnScreenWithoutTouchingTheAuthorizationNotice()` — call `model.show(notice: "Add your webhook URL and sender key in Settings — 2 ping(s) are waiting to be sent.")` and assert `model.guidance` is that sentence, `model.authorizationNotice` is unchanged, and the log is untouched. This is the entry point 03-10's drain notice arrives through; without a test it is a method nothing establishes works.</action>
  <verify>`SMOKE_DERIVED_DATA=build/dd-03-07 ./scripts/smoke.sh` exits 0 AND `build/dd-03-07.log` contains `Test aQueuedRowFlipsToSentWhenTheDrainReportsIt() passed` — a line only the Swift Testing runner emits, naming the test declared above. (If this toolchain's per-test line differs, grep the runner's own pass line for that name; never `BUILD SUCCEEDED` or a count.)</verify>
  <falsify>Make `PingHistoryLog.apply` always `record` instead of replacing a known id: `aQueuedRowFlipsToSentWhenTheDrainReportsIt` fails on the row count (two rows, not one). Revert.</falsify>
  <done>The Queued outcome and the in-place flip are both proven with no network, no disk and no view.</done>
</task>
</tasks>
