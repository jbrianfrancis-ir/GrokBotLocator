---
phase: 03-durable-delivery
plan: 01
wave: 2
depends_on: [03-11]
files_modified: [src/Core/Ping/PingPayload.swift, src/Core/Location/LocationFix.swift, tests/PingPayloadTests.swift, tests/LocationAuthorizationTests.swift, tests/PingTransportTests.swift]
autonomous: true
requirements: [REQ-02, REQ-05]
must_haves:
  truths:
    - "The wire body is exactly five keys — lat, lng, accuracy_m, label, at — in that order, and nothing else"
    - "`at` is the ISO-8601 UTC instant of the FIX, not of the POST and not of the encode: a payload built from an hour-old fix still carries that hour-old instant when encoded now"
    - "A PingPayload cannot be constructed without a capture time — no default parameter, no fallback to Date()"
    - "encoded() stays byte-stable: the same payload encodes to identical bytes on every call"
  artifacts: [src/Core/Ping/PingPayload.swift, tests/PingPayloadTests.swift]
  key_links:
    - "LocationFix.payload(label:) carries LocationFix.timestamp into PingPayload.capturedAt — the only bridge, so the fix time is the only thing that can reach `at`"
---

<objective>
D-12's fifth wire key. The queue this phase builds delivers a ping a wake later, so the
receiver must be able to tell when the fix was actually taken. This is the contract change.
</objective>

<context>
.planning/ARCHITECTURE.md "Wire format (pinned)" and "Smoke" — the byte-for-byte contract.
.planning/DECISIONS.md 2026-09-10 19:10 (D-12) — why `at` exists and why it lands here.
src/Core/Ping/PingPayload.swift (composes the bytes by hand; JSONEncoder key order is
non-deterministic — see .planning/LEARNINGS.md), src/Core/Location/LocationFix.swift.
EIGHT literal `PingPayload(...)` call sites break when `capturedAt` arrives, and Task 1 repairs
every one of them in the same task and the same commit — a plan whose intermediate state cannot
compile cannot have a green gate: tests/PingPayloadTests.swift:10, :64, :79, :100, :103, :106;
tests/LocationAuthorizationTests.swift:51; tests/PingTransportTests.swift:108. No production
code besides LocationFix builds one.
Gate: SMOKE_DERIVED_DATA=build/dd-03-01 ./scripts/smoke.sh. Swift Testing, never XCTest.
</context>

<tasks>
<task type="auto">
  <name>Task 1: capturedAt on PingPayload, the fix time that feeds it, and every call site repaired</name>
  <files>src/Core/Ping/PingPayload.swift, src/Core/Location/LocationFix.swift, tests/PingPayloadTests.swift, tests/LocationAuthorizationTests.swift, tests/PingTransportTests.swift</files>
  <action>One task, one commit, because the type change and the call-site repairs cannot be separated without leaving a tree that does not build.

In `PingPayload`, add `let capturedAt: Date` as the LAST stored property (after `label`), so the synthesized memberwise initialiser is `PingPayload(latitude:longitude:accuracyMetres:label:capturedAt:)`. Do NOT give it a default value and do NOT write an explicit initialiser that defaults it: a payload with a silently-invented timestamp is exactly the fallback ARCHITECTURE's "fail fast — no fallback values" rule forbids, and it would let a future call site forget the fix time. Add `case capturedAt = "at"` to `CodingKeys`, last.

In `encoded()`, render the instant with a pinned format style — `capturedAt.formatted(Date.ISO8601FormatStyle(timeZone: .gmt))`, which yields `2026-09-10T19:38:22Z` — and escape the resulting string through the SAME single-element-array trick the file already uses for `label` (encode `[atText]`, drop the outer brackets), so exactly one rule governs string escaping in this file. Append the key to the composed literal so the object reads
`{"lat":…,"lng":…,"accuracy_m":…,"label":…,"at":"…"}` — `at` LAST, after `label`.
The non-finite guard is unchanged; a `Date` has nothing to guard.

In `LocationFix.payload(label:)`, pass `capturedAt: timestamp`. Update the doc comment to say the fix's own timestamp is what reaches the wire as `at`, and that this bridge is the only place that can supply it. Update `PingPayload`'s header comment to name five keys, not four.

Then repair all eight literal call sites so the tree compiles again, adding `capturedAt:` to each:
- tests/PingPayloadTests.swift — give the shared `fixture` (line ~10) `capturedAt: Date(timeIntervalSince1970: 1_700_000_000)`, and add the same argument to the payloads at ~64, ~79, ~100, ~103 and ~106 (any fixed date; those tests are about labels and non-finite numbers);
- tests/LocationAuthorizationTests.swift:51 — the expected payload must use the SAME timestamp as the `LocationFix` it is compared against, which is what makes that existing assertion prove the bridge carries it;
- tests/PingTransportTests.swift:108 — any fixed date.
No assertion changes in this task; Task 2 adds those.</action>
  <verify>`SMOKE_DERIVED_DATA=build/dd-03-01 ./scripts/smoke.sh` exits 0 (the tree compiles and the existing 84 tests still pass), and `grep -c 'Date()' src/Core/Ping/PingPayload.swift src/Core/Location/LocationFix.swift` reports 0 for both files — neither type reads a clock of its own.</verify>
  <falsify>Change `payload(label:)` to pass `capturedAt: Date()` instead of `timestamp`: the grep finds it and the guard fails. Revert.</falsify>
  <done>Five keys in ARCHITECTURE's order, the timestamp comes from the fix and from nowhere else, and the tree builds green at this commit.</done>
</task>

<task type="auto">
  <name>Task 2: pin the five-key format and the fix-time semantics</name>
  <files>tests/PingPayloadTests.swift, tests/LocationAuthorizationTests.swift</files>
  <action>Now that everything compiles, add the assertions that pin the new contract. In `PingPayloadTests` (whose `fixture` now carries `capturedAt: Date(timeIntervalSince1970: 1_700_000_000)` = `2023-11-14T22:13:20Z`):
- rename `encodesExactlyTheFourKeysWithTheRightTypes` to `encodesExactlyTheFiveKeysWithTheRightTypes`, assert `Set(dict.keys) == ["lat","lng","accuracy_m","label","at"]`, that `dict["at"] is String`, and that `dict["at"] as? String == "2023-11-14T22:13:20Z"` — the literal, not a re-derivation through the same format style, so a format change fails the test instead of moving with it;
- extend `keysAppearInTheOrderArchitectureNames` with `"at"`, asserting `labelRange.lowerBound < atRange.lowerBound`;
- add `@Test func atCarriesTheFixTimeNotTheEncodeTime()` — build a payload whose `capturedAt` is `Date(timeIntervalSince1970: 1_700_000_000)`, encode it, and assert the emitted `at` is `2023-11-14T22:13:20Z` and is NOT within a minute of `Date()`. This is the test that catches someone reaching for the current time at encode time.
In tests/LocationAuthorizationTests.swift, add an explicit assertion beside the existing equality that `fix.payload(label:).capturedAt == fix.timestamp`, so the bridge is pinned by a named claim rather than only by whole-value equality.</action>
  <verify>`SMOKE_DERIVED_DATA=build/dd-03-01 ./scripts/smoke.sh` exits 0 AND `build/dd-03-01.log` contains `Test atCarriesTheFixTimeNotTheEncodeTime() passed` — a line only the Swift Testing runner emits, naming the test declared above. (If this toolchain's per-test line differs, grep the runner's own pass line for that name; never `BUILD SUCCEEDED`, `** TEST SUCCEEDED **`, or a test count.)</verify>
  <falsify>In `encoded()`, replace the `capturedAt` interpolation with `Date()`: `atCarriesTheFixTimeNotTheEncodeTime` fails and the log shows that test failing rather than passing. Revert.</falsify>
  <done>The five-key format and the fix-time semantics are both pinned by a test that fails if either drifts.</done>
</task>
</tasks>
