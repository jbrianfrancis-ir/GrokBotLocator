# Xcode / generated project
*.xcodeproj/
*.xcworkspace/
DerivedData/
build/
*.xcuserstate
xcuserdata/

# xcodegen `info:` block fully regenerates this from project.yml on every `generate` -- a
# manual edit is silently overwritten (verified), so project.yml stays the source of truth.
GrokBotLocator/Info.plist

# Signing — team id, bundle id, and profiles never enter the repo
Signing.xcconfig
*.mobileprovision
*.p12
*.cer

# macOS
.DS_Store

# nodeterm canvas state (machine-local)
.nodeterm/
